Read complete (closure paged in full: 16 dimensions, 17 classes, FOC-04 checklist, changelog). Fresh-context completeness verdict follows.

# STAGE C ROUND 1 — COMPLETENESS CRITIC VERDICT

**Answer: NOT NONE. Two missing challengers survive construction; no third exists. Additionally, one class (K-TLOG) and one cross-cutting axis pattern (AX-15/AX-18/AX-12 substrate-proof asymmetry) warrant re-steelman.**

---

## CHALLENGER 1 — K-DMERGE: Deterministic-Merge Admission Fabric (Mode C). STRONGEST FINDING — this is a defect internal to the closure's own grammar.

The closure itself (DIM-05, CA P1-7) enumerates and **adjudicates as SURVIVING** the family `deterministic-merge-CRDT-with-admission-at-merge`: every operation passes the single decidable admission predicate AT the deterministic total-order merge point before becoming canonical; canonical state a pure function of the committed operation set; "QUALIFIES [under frozen H4] … structurally distinct from shipped CRDTs. No shipped member instantiates it; K-CRDT's members do not." Yet **no U_T class carries this family in its signature** — all nine steelmen sit on single-writer-kernel-arbitration and/or bft-replicated-total-order. A surviving, H-admissible, structurally-distinct family with no class seat is by definition not one of the 9 and not reducible to them.

- **(a)** Not one of the 9: verified against all nine signatures; absent from every one.
- **(b)** Not reducible: not to K-CRDT (D_T) — the closure's own elimination lemma says the CRDT elimination "does NOT cover" this family (K-CRDT members are admission-after-acceptance); not to K-BFT — no consensus round, no quorum liveness dependency; not to Mode A — no physical writer seat. The closure explicitly built the re-adjudication path ("a future member instantiating [it] would … enter U_T — the taxonomy now expresses that path instead of foreclosing it") and then the steelman stage foreclosed it by omission.
- **(c)** Plausibly-unique ceiling rungs: **AX-15 is field-unreached (max R4, "no verified recovery procedure exists in any class")**. K-DMERGE's recovery story is structurally different from both modes: recovery = re-merge of the committed op set — crash-ordering reasoning is eliminated as a class, reduced to a once-proven merge-determinism theorem. And the substrate-class mechanized artifact **already exists**: Gomes et al.'s Isabelle/HOL-mechanized strong-eventual-consistency proofs for CRDT merge functions — precisely the same evidential move (existing mechanized substrate-class proof) that earned K-BFT its unique AX-03 R5 via Velisarios/ACL2. Secondary unique territory: k-replica durability-at-ack without total-order latency, and partition/offline liveness (AX-16 attainment under network conditions where Mode B provably loses liveness and Mode A loses availability) — a joint {durability, partition-liveness, H4/H5 compliance} cell neither pinned mode can occupy.
- **Consequence for the round-1 report**: synthesis obligation (b) is framed as a **binary** Mode A × Mode B choice. The closure's own grammar contains Mode C. Under CEILING-BAR, pinning A or B now requires a frozen impossibility-under-chosen-model note for Mode C's rungs too — which cannot be written until Mode C has been steelmanned. Obligation (b) as stated is unclosable without this challenger entering the field (as a 10th profile or as a formally adjudicated third mode within the join).

## CHALLENGER 2 — K-ZKREC: Transparent Whole-Pipeline Proof-of-Recomputation spine (succinct self-verifying institution).

Deterministic trusted kernel compiled to a zkVM (RISC Zero/SP1-class, STARK, no trusted setup); every canonical transition and the whole history ships with a transparent succinct proof; any third party verifies the entire institutional history in polylog time from the committed head, with the verifier's cost bound a theorem of the proof system.

- **(a)** Not one of the 9: no signature contains whole-pipeline arithmetization.
- **(b)** Not reducible — by the closure's own scope fence: CA P2-14 explicitly qualifies `proof-carrying-state-updates` as covering "update-validity predicates over bounded structures, **NOT whole-pipeline arithmetization**." So the only family in its vicinity is declared not to contain it. The closure simultaneously declares transparent proof-of-recomputation "equally maximal in assurance" with recompute-and-compare (same hash-assumption family) and says any exclusion is RESOURCE_BOUNDED toolchain cost — and CEILING-BAR rules cost inadmissible. Feasibility-today: zkVMs prove real deterministic RISC-V programs now; the H5-pure kernel is exactly the workload shape they handle.
- **(c)** Unique rung: **AX-18 is field-unreached (max R3, "no machine-checked scaling bound artifact exists")**. A STARK verifier IS a machine-checked sublinear verification-cost bound — the exact missing artifact type. It also breaks AX-13's linear-recompute verification floor (D_T verification cost currently grows linearly with history) and upgrades K-TLOG's portable countersigned receipt into a *self-verifying* receipt (no re-execution capacity required of the verifier — materially widening the class of parties who can hold the institution to H8).
- **Honest caveat, stated**: K-ZKREC's most natural disposition may be absorption — a new DIM-06/DIM-08 family + mandatory E★★ v2 strengthener — rather than a 10th class. But under the report's own rules it must then appear in §3A/§3B; **it currently appears in neither list**, so the round-1 report has a completeness defect either way: file it as a challenger or add the absorption entry.

## NO THIRD CHALLENGER — declaration with sweep record.

Candidates constructed and rejected: **MPC/threshold computation** (adds only confidentiality; the 22-axis instrument has no confidentiality axis, so no ceiling rung exists for it to reach — see taxonomy note below; integrity-wise reducible to K-BFT); **optimistic/fraud-proof verification** (accept-then-challenge trusts a declared root during the window — H8-inferior, dominated); **verified-runtime shield over an untrusted core** (is K-NSV's verify-out shape / K-COM+'s rim); **mechanized human-panel adjudication** (absorbed: accountable-human-decision-authority protocol objects, DIM-05); **rules-as-code-at-promulgation institution** (is K-CAT's publisher-authoritative DIM-16 cell, obligation (g)); **certified incremental view maintenance** (weaker conception of Challenger 2's sublinear-verification cell; subsumed under the supreme law); **total precomputation of a finite query grammar** (K-CAT/K-RULES + K-TLOG static tiles); **CHERI/capability hardware** (tcb-minimized-root family); **CNL/controlled-natural-language** (round-trip-verbalization-invariant, DIM-16); **any statistically-trusted class** (outside U_T by H1/H2 construction, cannot reach assurance ceilings under H8); **federated multi-firm interop** (SCITT, inside K-TLOG). None satisfies (a)+(b)+(c) simultaneously.

## RE-STEELMAN FINDINGS

**1. K-TLOG — YES, re-steelman required (underplayed by construction).** Its closure definition is "Same trusted symbolic core options as K-ARG/K-CAT, distinguished on DIM-09…" — a steelman was therefore licensed to pin the strongest surviving core and inherit its rungs. Instead it was adjudicated core-less: 5 AT_CEILING, 8 UNKNOWN, with §5 showing its UNKNOWNs (AX-01, AX-05, AX-08, AX-09, AX-19…) are mostly "the core is unbuilt" — a status the class definition did not force. Additionally it missed two existing-artifact moves: the F*-verified Merkle tree (HACL*/CCF lineage) with certified O(log n) inclusion/consistency verification toward **AX-18** (the field-unreached axis), and Cedar-in-Lean (an existing machine-checked authorization model, absorbed in DIM-05's family text) composed with its verifiable-authority-directory toward **AX-12** (field-unreached, "verified authority model exists nowhere").

**2. Cross-cutting asymmetry — the Velisarios precedent was applied once and then never again; a uniform ruling is required.** K-BFT's unique AX-03 R5 rests on *existing mechanized proofs for the substrate class* (Velisarios/ACL2/Apalache), not on proofs of its own unbuilt instance. The identical move was available and un-run on: **AX-15** — Perennial/GoJournal crash-Hoare-logic proofs, *explicitly absorbed into the atomic-single-commit-point-store family* carried by K-SUP, K-CAT, K-NSV, K-ARG, yet the report records "no verified recovery procedure exists in any class"; **AX-18** — verified Merkle verifier bounds (above); **AX-12** — Cedar-in-Lean (above); and Challenger 1's AX-15 via Isabelle-mechanized SEC proofs. Round 2 must rule ONE way: either substrate-class mechanized proofs ground rungs uniformly (then the AX-15/AX-18/AX-12 field maxima in §1 are understated and those cells re-adjudicate upward), or they do not (then K-BFT's AX-03 R5 — the field's only one — falls to the same standard). The current record is inconsistent on exactly the cells the report calls the E★★ frontier.

**3. No other steelman requires re-steelman.** K-RULES's 1-AT profile is the designed control ("expected to LOSE" — FOC-04 checklist), not an underplay. K-B0-TARGET's UNKNOWNs are evidence-blocked per the frozen evidence discipline, correctly so. K-SUP/K-NSV/K-CBR/K-CAT/K-ARG/K-BFT profiles are consistent with their closure signatures.

**Taxonomy note for the freeze authority (not a challenger, no existing rung to reach):** the 22-axis instrument contains **no confidentiality/professional-privilege axis** — client secrecy appears only obliquely via erasure-compatible-committed-memory (DIM-07). For a legal institution this is a plausible instrument gap; it changes no round-1 verdict (challengers must reach *existing* ceiling rungs) but should be recorded alongside the S1 taxonomy notes in §7.5.

**Net effect on the round-1 report:** §7's round-2 mandate is incomplete as written — it must add: (i) K-DMERGE steelmanned as a 10th profile or as the formally adjudicated third branch of obligation (b) (whose binary framing is hereby refuted); (ii) K-ZKREC filed either as challenger or as a mandatory §3A/§3B absorption with AX-18 seat; (iii) the substrate-proof uniformity ruling before any AX-03/AX-15/AX-18/AX-12 cell re-freezes; (iv) K-TLOG re-steelman with a pinned core.