# THE LEGAL WATCHTOWER — pre-Phase-2 freeze v2-R3

This is one canonical supervisory bundle outside the Andrianna repository. It
contains two role-separated transport capsules:

- `PHASE-2-BLIND-PRODUCER-INPUT.zip` — the only file the fresh Phase-2 producer may receive.
- `PHASE-2-REVIEWER-FROZEN-GATES.zip` — keep hidden until the producer stops and its output hash is fixed.

The repository `andriannadeepmind-gif/THE-LEGAL-WATCHTOWER` remains untouched.

## Windows placement

Extract this outer bundle to:

```text
C:\THE-LEGAL-WATCHTOWER-STUDY-OUTPUT\PRE-PHASE-2-FREEZE-v2-R3\
```

Do not place either capsule inside the Andrianna repo or inside a Phase-2 output
directory.

## What is frozen

The rules, not the result, are frozen before Phase 2:

- answer-neutral Phase-2 evidence and review criteria;
- producer/reviewer separation and no self-certification;
- FOC-15 pre-Phase-3 freeze ordering;
- FOC-16 universal escalation;
- FOC-17 proof-carrying continual evolution and monotonic capability ratchet;
- FOC-18 strict componentwise superiority over CoCounsel Legal, Lexis+ with
  Protégé and Harvey on every one of 22 frozen architecture-and-mechanism axes;
- T6 requires all 66 product-by-axis cells to be `STRICTLY_BETTER` and each
  commercial capability surface to be a proper subset of the final executable;
- FOC-19/T7 require that final executable to be the exact verified evolutionary
  successor implemented in the Andrianna repository, with a total post-reveal
  file/package/symbol/store/gate changeset, capability-preservation matrix and
  migration proof; a disconnected greenfield or parallel system cannot pass;
- equality, averaging, weighting, compensation, trade-offs, content advantage
  and `UNKNOWN` are forbidden substitutes for commercial superiority;
- `RED_TEAM_TERMINATION = UNIVERSAL_PROOF_ONLY`;
- the final greatest-element, uniqueness and executable-refinement proof gate;
- fail-closed result `FINAL_OPTIMALITY_BLOCKED` whenever universal proof is absent.

Blindness is now a separate provenance label. A missing transcript can make the
label `BLINDNESS_UNVERIFIED`, but it does not force an endless Phase-2 rerun and
cannot establish or refute the final mathematical theorem.

## Before launch

1. Run `python verify_bundle.py`; it must report `OUTER_FREEZE_VALID`.
2. Extract `PHASE-2-REVIEWER-FROZEN-GATES.zip` locally for inspection only and run:

   ```text
   python verify_freeze.py
   node verify_freeze.mjs
   ```

   Both must report `FREEZE_STRUCTURE_VALID`.
3. Approve the three immutable hashes printed by `verify_bundle.py`. The approval
   receipt is external; no file is edited or resealed.

## Launch

Never use the current Claude Code session connected to the Andrianna repo.

Create a new upload-only Code workspace with no repository. If the platform
requires a repository, use a new sterile study-only repository with no fork,
mirror, import, template, submodule, object database, history or remote relation to
Andrianna. Start a genuinely new session — not resume, continue, branch, fork or an
inherited subagent.

Upload only:

```text
PHASE-2-BLIND-PRODUCER-INPUT.zip
```

Paste exactly:

```text
Extract the attached blind-input capsule in this new empty workspace. Read
START-FRESH-PHASE-2.md and execute it exactly. Before research, perform the
isolation preflight and stop before architecture work if prior project memory, a
baseline repository, an earlier study artifact, reviewer material or inherited
session context is visible. Phase 2 only. Deliver one sealed submission ZIP and
do not begin Phase 3.
```

If an isolation evidence gap is discovered only after useful architecture work,
preserve the result and label it `BLINDNESS_UNVERIFIED`; do not rerun Phase 2 just
to improve the label.

## Review and later phases

After the producer stops, record its ZIP bytes/SHA-256 and preserve the full
session. Then use a distinct reviewer session with the hidden reviewer capsule.
Phase 2 can become `PHASE_2_CANDIDATE_ACCEPTED_FOR_PROOF_PIPELINE`; it cannot prove
the ceiling.

The final success status is possible only after all architecture classes are
covered or bounded and the mechanically checked theorem proves that the executable
Omega is at least as strong as every feasible member, unique up to material
equivalence, strictly better than every named commercial baseline on every frozen
mechanism axis, a proper capability superset of each, and independently reproduced.
It must also be built from B0 through the creator-approved exact repository delta;
the study must state precisely what is retained, added, modified, refactored,
moved, replaced and removed and verify the resulting Andrianna commit as E_star.
Thereafter, any validated new challenger or named-product update
automatically suspends the current-ceiling claim until a non-regressive successor is
proved and independently reproduced. Otherwise the honest status is
`FINAL_OPTIMALITY_BLOCKED`.
