# V1.3 KILL WITNESSES — ΠΡΟΔΗΛΩΜΕΝΟΙ, ΜΗ ΕΚΤΕΛΕΣΜΕΝΟΙ

**ΚΑΤΑΣΤΑΣΗ: ΠΡΟΔΗΛΩΜΕΝΟΙ — ΔΕΝ ΕΧΟΥΝ ΕΚΤΕΛΕΣΤΕΙ. ΚΑΝΕΝΑ DESTRUCTION PASS.**

Προδήλωση kill witnesses **πριν** από το destruction pass — ώστε, όταν ο δημιουργός
δώσει ρητή εντολή, ο αντίπαλος να μην ξοδέψει γύρο σε ήδη γνωστούς falsifiers.
Design only. Καμία εκτέλεση, καμία αξίωση επιτυχίας/αποτυχίας.

**Κανόνας (KernelL1 μάθημα):** κάθε witness ορίζει την **ακριβή μετάλλαξη** που
ΠΡΕΠΕΙ να κοκκινίσει και το **αναμενόμενο typed error** (MLTP §4 error taxonomy).
Witness που δεν σπάει τη μεταλλαγμένη σχεδίαση = ψευδής — ο ίδιος ο witness είναι
προς έλεγχο στο destruction pass.

---

| KW | άξονας | μετάλλαξη (stand-up) | αναμενόμενο (want) | έδρα κλεισίματος στο v1.3 |
|---|---|---|---|---|
| **KW-1** | issuer self-verdict | `IssuedClaim` που φέρει `verification_result: "VERIFIED"` υπογεγραμμένο από τον εκδότη | **reject** — self-verdict· το αποτέλεσμα ζει μόνο στο `VerificationReceipt` (Layer C) | MLTP §1.0, §7 |
| **KW-2** | SHA-only vs RS256/Ed25519 | verifier που «επαληθεύει» RS256/Ed25519 υπογραφή περνώντας **μόνο** SHA-256 (δεν ελέγχει την υπογραφή) απέναντι σε πλαστή υπογραφή | **sig-invalid** — inclusion (SHA-256) ΔΕΝ αρκεί για signature· απαιτείται RS256/Ed25519 | MLTP §4, §8 (B) |
| **KW-3** | raw-byte identity churn | εισαγωγή δεύτερου manifestation/κανάλι της ίδιας απόφασης· σχεδίαση που παράγει **δεύτερο** `work_id`/`expression_id` από τα raw bytes | **W-UNRELATED-CORPUS-IDENTITY-CHURN** — μία νομική ταυτότητα, πολλά items· ταυτότητα ΟΧΙ από raw-byte digest | v1.3 §2.1, MLTP §2.5, Q13/Q24 |
| **KW-4** | RFC-3161 fake provenance | αντικείμενο με έγκυρο RFC-3161 `time_anchor` αλλά **χωρίς** `authority_proof_ref`+`institutional_register_id`, παρουσιασμένο ως `RELEASED` | **insufficient-provenance** — χρόνος bytes ≠ επίσημη προέλευση | v1.3 §2.2, MLTP §2.1, Q03 |
| **KW-5** | split-view με συμφωνούντες embedded witnesses | δύο ασυνεπή `log_root` με **embedded** witness υπογραφές που «συμφωνούν» μεταξύ τους αλλά όχι με τον consumer checkpoint | **split-view** — consistency απέναντι στο **τελευταίο γνωστό** checkpoint του consumer (gossip), όχι στους embedded | MLTP §8 (D), §10 (transparency ≠ correctness) |
| **KW-6** | pre-revocation signature, retroactive compromise | υπογραφή με timestamp **πριν** το `revoked_at`, αλλά από κλειδί με `revocation_reason: key-compromise` και `invalid_from` **πριν** την υπογραφή | **retroactively-revoked** — «pre-revocation stays valid» ΔΕΝ ισχύει σε compromise· μόνο υπογραφές με ανεξάρτητο RFC-3161 χρόνο πριν το `invalid_from` επιβιώνουν | MLTP §9, §2.8 |
| **KW-7** | interpretive ratio ως source fact | `jurisprudential-analysis` όπου raw AI inference (`PLANE-3`) παρουσιάζεται ως πιστοποιημένο `ratio`, χωρίς `passage_anchor`/`reviewer_adoption_act` | **unadopted-analysis** / `UNVERIFIED_FOR_MACHINE_RELIANCE` — ratio απαιτεί passage anchor + attribution + methodology + reviewer adoption· AI inference ΠΟΤΕ ως θεσμικό ratio | v1.3 §5, MLTP §2.6 (split από §2.5) |
| **KW-8** | stale v1.2 qualification rule | κανόνας/έγγραφο που εφαρμόζει την **παλιά** v1.2 σημασιολογία (π.χ. «ταυτότητα = PLANE-0 digest», «RFC-3161 = αυθεντικότητα», «Root Authority = μόνιμη», «SHA-256 μόνο», Q03/Q13 v1.2) σε τομή v1.3 | **conflict** — καμία v1.2 αδρανής σημασιολογία δεν ισχύει· ο `V1.3-CONSISTENCY-AUDIT.md` το μηδενίζει μηχανικά | v1.3 §1, CONSISTENCY-AUDIT |
| **KW-9** | delegated key ≡ root (λάθος σύγκριση) | verifier που ελέγχει `thumbprint(delegated_release_key) == pinned_owner_root` | **λάθος verifier** — το delegated key έχει ΔΙΑΦΟΡΕΤΙΚΟ thumbprint· η αλυσίδα είναι root → signed delegation → delegated key → claim· ο σωστός verifier ΔΕΝ κάνει αυτή τη σύγκριση | MLTP §8.2 |
| **KW-10** | έγκυρο κλειδί, λάθος scope | delegated key με scope `{release-signing}` υπογράφει `jurisprudential-analysis` IssuedClaim | **delegation-scope-violation** — scope ελέγχεται έναντι `claim_type` | MLTP §8.2, §8.3 (B) |
| **KW-11** | dangling `qualification_state_ref` | IssuedClaim με `qualification_state_ref` που **δεν** επιλύεται σε `qualification_records` του bundle ούτε με tlog inclusion | **dangling-qualification-ref** ⇒ level `none` — ποτέ σιωπηλή αποδοχή | MLTP §3, §6, §8.3 (H) |
| **KW-12** | self-qualified release issuer | `QualificationStateRecord` υπογεγραμμένο από την **release-authority** kid (ή role `release-issuer`) που χαρακτηρίζει το δικό της release `mission-qualified` | **unauthorized-qualification-issuer** — μόνο independent-auditor/quorum/provider-registry· ο εκδότης ποτέ για τον εαυτό του | MLTP §3, §8.3 (H) |
| **KW-13** | stopped/rewound clock | `LocalTrustState.trusted_time.evidence = null` (ή rewound ρολόι) ενώ το bundle φέρει ληγμένο `expiry`· verifier που επιστρέφει `VERIFIED` με βάση το τοπικό ρολόι | **UNKNOWN_FRESHNESS** — ιστορική υπογραφή ΜΠΟΡΕΙ να επαληθευτεί, freshness/expiry ΔΕΝ κρίνεται· ποτέ `VERIFIED` (κλείνει KT1) | MLTP §8.1, §8.3 (F, J) |
| **KW-14** | revocation στο legal effective-time | υπογραφή με `t_sig ≥ invalid_from` αλλά payload `valid_time` **πριν** το `invalid_from`· verifier που ελέγχει revocation έναντι `valid_time`/`effective_time` | **retroactively-revoked** — η ανάκληση ελέγχεται έναντι **trusted signature time** (`issued_at`), ΟΧΙ legal effective-time | MLTP §1.0, §8.3 (G), §9 |
| **KW-15** | πλαστό embedded registry | bundle με `embedded_registries.witness_keys` που περιέχουν πλαστούς witnesses (μόνο στο bundle), «συμφωνούντες» μεταξύ τους | **untrusted-registry** — embedded keys UNTRUSTED μέχρι επίλυση μέσω `LocalTrustState`· witness που υπάρχει μόνο στο bundle ΔΕΝ μετρά για quorum | MLTP §6, §8.3 (D, E) |
| **KW-16** | Key Lifecycle ↔ MLTP v2 αντίφαση | ίδια υπογραφή: KEY-LIFECYCLE §2.5 «έγκυρη» (πριν revoked_at + RFC-3161) vs MLTP v2 §9 «retroactively-revoked» (t_sig ≥ invalid_from, compromise) | **μία ετυμηγορία** — versioned precedence: MLTP v2 §9 υπερισχύει για key-compromise· KEY-LIFECYCLE §2.5 φέρει την παραπομπή· δύο ACTIVE specs ΔΕΝ διαφωνούν | MLTP §9, KEY-LIFECYCLE §2.5 |

---

## ΤΙ ΔΕΝ ΚΑΝΕΙ ΑΥΤΟ ΤΟ ΑΡΧΕΙΟ

- **Δεν** εκτελεί κανένα witness — προδήλωση μόνο.
- **Δεν** αποδεικνύει ότι η σχεδίαση επιβιώνει· αποδεικνύει ότι, όταν δοθεί εντολή,
  ο αντίπαλος **ξεκινά** από αυτούς τους 16 (KW-1…KW-16, **υποχρεωτική βάση**) και
  ψάχνει **πέρα** από αυτούς (άγνωστοι νέοι άξονες = η πραγματική δουλειά του
  destruction pass). Default verdict: `FALSIFIED` όταν η επιβίωση δεν αποδεικνύεται.
- **Δεν** υποκαθιστά το destruction pass. Το προετοιμάζει.
