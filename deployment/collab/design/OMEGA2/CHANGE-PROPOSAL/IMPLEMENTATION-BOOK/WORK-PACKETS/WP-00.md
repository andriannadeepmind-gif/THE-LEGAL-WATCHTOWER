# WP-00 — Clean reproducible base + genuinely green CI

**Bound `88129099` · EXECUTION NOT AUTHORIZED.** Executable without new design decision.

- **Requirement:** AS-IS R-1..R-6 (U-8); R-85, R-86, R-87, R-88, R-99, R-100 (§4.14 build/CI/SBOM/provenance). Prerequisite of every WP.
- **Rationale:** No implementation claim may rest on a base that does not build reproducibly and has never had a genuinely green CI (AS-IS EV-12: 71 runs, 0 successes). Close that before any feature WP.
- **Architecture seat:** S11/S15 substrate + reproducible substrate (L11). Frozen: v1.4 §4.14, `IMPLEMENTATION-SEQUENCE` βήμα 0.

### Elements (path · package/domain · disposition · public surface)
| element | path | domain | disp | surface |
|---|---|---|---|---|
| CI workflows | `.github/workflows/*.yml` | GitHub Actions | REPLACE | jobs emit `PASS`/`FAIL`/`BLOCKED` distinct exit codes; two independent runs |
| hermetic image | `Dockerfile`, `deps.lock`, `docker/sbom.json`, `docker/cosign.pub` | container | KEEP | byte-identical image digest ×2 |
| provenance template | `authority-v2/run-all.sh` | script | KEEP | 0/1/3 exit template |
| stale-authority claim | `source/semantic-authority.lisp` sym `PRIMARY_SEMANTIC_AUTHORITY` | `orchestrator.semantic-authority` | REMOVE | claim removed (CAP-139, AS-IS EV-11); 0 occurrences in machine-readable output |
| AS-IS evidence | `AS-IS-EVIDENCE-MANIFEST.md` R-1..R-6 | doc | MODIFY | REPORTED → CONFIRMED/REFUTED with an executable test |

- **Contracts:** CI job result ∈ `{PASS, FAIL, BLOCKED}` (BLOCKED ≠ FAIL, environmental ≠ code); image digest (sha256); SBOM (SPDX/CycloneDX); cosign signature.
- **Ordered edits:** (1) rewrite each workflow to run one suite and map environmental failure → `BLOCKED` (exit 3), code failure → `FAIL` (exit 1); (2) remove the `PRIMARY_SEMANTIC_AUTHORITY` claim symbol and its callers; (3) add executable tests for AS-IS R-1..R-6; (4) wire SBOM emission + cosign signing into the release job; (5) record first SLO baseline (U-1 initial numbers).
- **Tests / kill tests:** Q18, Q40 · KW-59 · VS-15 prerequisite (hermetic build).
- **Commands:** `sbcl --script build.lisp` (build); `sbcl --load orchestrator-tests.asd --eval '(asdf:test-system :orchestrator-tests)'` (fiveam); `bash deployment/verify/mltp3/run.sh`; `docker build` twice → compare digests; `bash deployment/collab/design/OMEGA2/CHANGE-PROPOSAL/V1.4-CONTRADICTION-OMISSION-AUDIT.sh`.
- **Toolchain freeze:** Common Lisp = **SBCL** (already the repo engine, `orchestrator-engine-sbcl`); container = Docker + pinned `deps.lock`; signing = **cosign** (Sigstore); test = **fiveam**. **Decision gate WP-00-a:** exact pinned SBCL version + base image digest (criteria: reproducible, maintained LTS) — resolved before edit (1).
- **Migration:** none (base). Makes the image reproducible. **Rollback:** abandon branch pre-merge; post-merge restore prior image by digest; `deployment/self/history.sexp` and `output/.healthy` restored (`git checkout --`) before every commit — never touched by the human flow.
- **Evidence:** two green CI run URLs; two identical image digests; SBOM; cosign signature; U-8 6/6 CONFIRMED/REFUTED; `PRIMARY_SEMANTIC_AUTHORITY`=0 in machine output; SLO baseline numbers.
- **Paper dry-run (VS-15 prereq):** input = the repo at frozen SHA; expected = 2×2 green CI + byte-identical digests; failure path = digest drift ⇒ `Q40` red (non-hermetic) ⇒ WP does not exit; rollback = re-pin deps and rebuild.
- **Binary exit gate:** every workflow PASS on two independent runs **AND** image digest identical ×2 **AND** U-8 closed **AND** `PRIMARY_SEMANTIC_AUTHORITY`=0. Else BLOCKED.
