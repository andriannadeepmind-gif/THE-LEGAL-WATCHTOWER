# WP-01 — National source/court census + coverage ledger

**Bound `88129099` · EXECUTION NOT AUTHORIZED.** Entry gate = WP-00 exit.

- **Requirement:** R-01..R-13 (§4.1 census/radar), R-132 (§4.20 source-type registry). MIS-2. closure-WP-01/16.
- **Rationale:** A declared, root-signed census universe with a **total** coverage function makes "silent loss structurally impossible": every expected object has exactly one state (INGESTED/EXPLICITLY-ABSENT/QUARANTINED/UNKNOWN), never a gap that simply "is not there" (KW-48).
- **Architecture seat:** S1 Census/Radar + S16 Source-type registry. Frozen: v1.4 §4.1, `LAWMAX-PUBLIC-SOURCE-TYPE-AUTHORITY-REGISTRY.md`.

### Elements
| element | path | package (AS-IS) | disp | surface |
|---|---|---|---|---|
| radar/enumerator | `source/ingestion-daemon.lisp`, `source/legislation-ingestion.lisp`, `source/government-source.lisp`, `source/document-fetch.lisp` | `orchestrator.*` (AS-IS-FILE-INVENTORY.tsv) | MODIFY | enumerator `issue × year × number` per census space |
| source profile | `source/source-profile.lisp` | `orchestrator.source-profile` | KEEP | ranked official channels |
| capability/gap ledger | `source/capability-registry.lisp` | `orchestrator.capability` | MODIFY | gap ledger as total function |
| coverage ledger | **NEW** `source/coverage-ledger.lisp` | `orchestrator.coverage` | NEW | `coverage-state(space, position) → {INGESTED|ABSENT|QUARANTINED|UNKNOWN(gap_reason,retry_state)}` (total) |
| census snapshot | **NEW** `RegistrySnapshot` producer | `orchestrator.census` | NEW | owner-root-signed `RegistrySnapshot` (MLTP §2.9) |
| coverage claim | **NEW** `coverage-and-freshness/1` | contract type | NEW | `{universe_declaration_ref, covered, gaps[], freshness_budget}` |

- **Contracts:** census space id `gr/<series>` (e.g. `gr/gazette/A`); `RegistrySnapshot{snapshot_root, spaces[], signed_by}`; `coverage-and-freshness/1`; `UNKNOWN` carries `gap_reason` + `retry_state`.
- **Ordered edits:** (1) define `coverage-ledger.lisp` as a **total** function over each census space (every position resolves to exactly one state); (2) extend the enumerators to enumerate `issue×year×number` from the signed snapshot, not from what was fetched; (3) define `RegistrySnapshot` producer signed by the owner-root ceremony rehearsal key; (4) emit `coverage-and-freshness/1`; (5) add a second independent enumeration path for the M-4 cross-check.
- **Tests / kill tests:** Q01, Q02, Q29 · KW-48 (ledger derived from fetched ⇒ red) · VS-13.
- **Commands:** `sbcl --load orchestrator-tests.asd --eval '(asdf:test-system :orchestrator-tests)'`; census enumeration CLI over `gr/gazette/A` 2025; second independent enumeration; diff = 0 unexplained.
- **Toolchain freeze:** SBCL; HTTP fetch = **drakma** (AS-IS); time = **local-time**; hashing = **ironclad** SHA-256 for ledger roots (maintained, non-trusted-path hashing). **Decision gate WP-01-a (external, U-7):** the authoritative list of courts whose decisions are lawfully publishable — closes at WP-01 exit. **WP-01-b (U-1):** per-space freshness budgets from measurement.
- **Migration:** additive root-signed snapshot (new census universe), forward-only; a new snapshot supersedes, the old stays versioned. **Rollback:** newer snapshot wins; abandon branch pre-merge; PLANE-0 untouched.
- **Evidence:** signed snapshot; per-position single state proof; second-enumeration diff (0 unexplained); VS-13 bundle.
- **Paper dry-run (VS-13):** input = `gr/gazette/A` 2025 with one issue removed from the fetch channel (declared mutation); expected = that position = `UNKNOWN(gap_reason, retry_state)` within budget, provider answer carries `coverage_state`, second enumeration agrees; failure path = ledger derived from fetched ⇒ position merely missing ⇒ KW-48 red ⇒ no exit; rollback = re-pin snapshot.
- **Binary exit gate:** total coverage (no silent loss) **AND** U-7 + U-1 closed **AND** VS-13 3/3 witnesses. Else BLOCKED.
