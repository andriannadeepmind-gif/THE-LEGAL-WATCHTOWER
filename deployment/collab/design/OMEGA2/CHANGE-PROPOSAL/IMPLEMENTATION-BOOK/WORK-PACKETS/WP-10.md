# WP-10 — National Legal Digital Twin + impact engine

**Bound `88129099` · EXECUTION NOT AUTHORIZED.** Entry gate = WP-04 exit.

- **Requirement:** R-48..R-50 (§4.8 normative-impact simulator). MIS-3. closure-WP-05/06. (Consumes R-35..R-38 event store; primary ownership WP-03.)
- **Rationale:** A corpus-wide `normative-impact-projection` with a `replay_manifest` lets any auditor re-derive the same `impact_root`; the public simulator is a **projection**, never a hypothesis-strategy engine (that is DEFER_PRIVATE).
- **Architecture seat:** S5 twin + S6 hypergraph (Legal Digital Twin, L7). Frozen: v1.4 §4.8/§4.5, USC §6.

### Elements
| element | path | package | disp | surface |
|---|---|---|---|---|
| impact reasoning | `source/graph-reasoning.lisp` | `orchestrator.graph` | MODIFY | `reason-impact` over a bitemporal cut |
| counterfactual/refs | `source/what-if.lisp`, `legal-counterfactual.lisp`, `legal-references.lisp`, `legal-hypergraph.lisp` | `orchestrator.*` | KEEP | public projection |
| EU impact | `source/eu-interop-layer.lisp` | `orchestrator.eu` | MODIFY | ELI-Impact projection |
| invalidation | `source/corpus-diff.lisp` | `orchestrator.corpus-diff` | MODIFY | invalidation set |
| impact profile | **NEW** `normative-impact-projection` + `replay_manifest` | contract | NEW | replayable projection |

- **Contracts:** `normative-impact-projection{impact_root, replay_manifest}`; `replay_manifest{inputs[], cut(v,k), steps[]}`; invalidation sets. The impact type has **no outcome field** (KW-54).
- **Forbidden dependencies:** public simulator ≠ private hypothesis strategy (I-4.8b, R-49); a type with an outcome field ⇒ compile failure (KW-54); site/API may not derive the twin (F4).
- **Ordered edits:** (1) `reason-impact` over a `(v,k)` cut; (2) `normative-impact-projection` profile + `replay_manifest`; (3) ELI-Impact RDF projection; (4) invalidation set from `corpus-diff`.
- **Tests / kill tests:** Q36 · KW-54 · VS-02(completion).
- **Commands:** `sbcl` impact fiveam suite; auditor re-run ⇒ same `impact_root`; SHACL on published ELI-Impact RDF.
- **Toolchain freeze:** SBCL; RDF = cxml/repo `.ttl`; SHACL validator. **Decision gate:** none.
- **Migration:** projections replayable from journal, forward-only. **Rollback:** projection module removable; revert with no source effect.
- **Evidence:** auditor re-run ⇒ identical `impact_root`; outcome-field type ⇒ compile failure; VS-02 complete.
- **Paper dry-run (VS-02 step 6):** input = the VS-02 journal; expected = `normative-impact-projection` with `replay_manifest`, `impact_root` identical on two runs; failure path = non-replayable ⇒ Q36 red; rollback = drop projection.
- **Binary exit gate:** `impact_root` replayable **AND** no outcome field in the type. Else BLOCKED.
