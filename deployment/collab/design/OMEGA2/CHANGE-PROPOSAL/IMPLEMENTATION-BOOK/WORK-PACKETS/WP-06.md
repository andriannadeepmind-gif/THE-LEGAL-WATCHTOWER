# WP-06 — MLTP v3, offline verifier (PCL-2), Trust Mesh + crypto agility

**Bound `88129099` · EXECUTION NOT AUTHORIZED.** Entry gate = WP-04 exit; U-2 closed.

- **Requirement:** R-57..R-70 (§4.10 MLTP/trust mesh), R-89/R-90/R-92/R-94 (§4.14 release-protection/least-privilege/secret-isolation/tamper), R-125..R-128 (executable closure), R-130 (§4.18 crypto agility), R-134 (§4.22 nation-state). MIS-1/MIS-9. closure-WP-10/11.
- **Rationale:** The trust layer already has an executed core (`deployment/verify/mltp3/run.sh`, exit 0). This WP raises it to production: threshold custody, two logs, witnesses, SCITT projection, a second (Rust) verifier, and crypto agility (policy epochs, ML-DSA PQ root, evidence renewal) — with **no homemade crypto**.
- **Architecture seat:** S10 MLTP trust layer + S11 security cells. Frozen: MLTP v3 (§13 executable, §14 agility), `LAWMAX-KEY-LIFECYCLE-SPEC.md`, `LAWMAX-TRUST-BOOTSTRAP-SPEC.md`.

### Elements
| element | path | domain | disp | surface |
|---|---|---|---|---|
| executed reference | `deployment/verify/mltp3/**` | Go/Node/Python | KEEP | acyclic core, 2 vetted verifiers, negatives 40/40 |
| authorities | `source/jws-authority.lisp`, `merkle-authority.lisp`, `hash-authority.lisp`, `x509-authority.lisp`, `asn1-der.lisp`, `timestamp-authority.lisp` | `orchestrator.*` | MODIFY | Ed25519, RFC 7638, TSR over signature |
| custody | `authority-v2/` | scripts | MODIFY | roles, witness-quorum, ceremony |
| verifiers | `deployment/verify/verify.py`, `verify.mjs`, `verify-release.py`, `verify-authority-bundle.py`, `kernel-verify.lisp` | multi | MODIFY | → MLTP v3 |
| PCL | `deployment/PROOF-CARRYING-LAW.md` | doc | MODIFY | → PCL-2 delegation-aware |
| threshold/logs/witnesses | **NEW** | custody | NEW | threshold signing, 2nd log, cross-client witness registry, SCITT projection, Rust verifier |
| crypto agility | **NEW** `crypto-policy-epoch`, `PQRootSet`, `evidence-renewal` | contract | NEW | suite registry, epochs, ML-DSA n-of-m, renewal chains |

- **Contracts:** `IssuedClaim`/`TrustBundle`/`VerificationReceipt`; `crypto-policy-epoch{suite, from, to, verifier_ref}`; `PQRootSet{members[], n, m}`; result sum `UNVERIFIED_FOR_MACHINE_RELIANCE < UNVERIFIED_FOR_ATTRIBUTED_RELIANCE < UNKNOWN < VERIFIED`.
- **Crypto (deliverable 8 — named, maintained, official vectors; no homemade):** classical signing = **Ed25519** — builder **libsodium 1.0.18** (via CFFI, the one foreign binding), verifier A **Go `crypto/ed25519`** (pure-Go, not OpenSSL), verifier B **Node/OpenSSL 3.x**; cross-check **RFC 8032 test vectors**. Timestamp = **RFC 3161** (real DER token). Envelope hash = SHA-256 (libsodium/RustCrypto/Go stdlib). PQ = **independent n-of-m ML-DSA multisignature** (FIPS 204), classical = **FROST-Ed25519**; hybrid **AND** (valid classical + invalid/missing PQ ⇒ reject, KW-104).
- **Decision gate WP-06-a (PQ library):** the ML-DSA backend — criteria: FIPS-204 conformant, **official NIST ACVP/KAT vectors** pass, maintained, no homemade primitive. **Decision gate WP-06-b (U-2):** the auditor/witness registries (institutional).
- **Forbidden dependencies:** no homemade cryptographic primitive anywhere on the trusted path; the two verifiers share no crypto backend (Go pure-Go vs Node/OpenSSL — N-version).
- **Ordered edits:** (1) raise `authority-v2/` to roles + witness-quorum + ceremony; (2) threshold signing + second log + cross-client witness registry + SCITT projection; (3) add the Rust verifier (third N-version); (4) `crypto-policy-epoch` + `PQRootSet` + `evidence-renewal`; (5) 35/35 error names each with a negative vector recognized identically by the three verifiers.
- **Tests / kill tests:** Q21, Q22, Q23, Q26, Q28, Q43 · KW-1/2/5/6/9..47, KW-64..106 · VS-11(checkpoint), VS-12.
- **Commands:** `bash deployment/verify/mltp3/run.sh` (exit 0); three-verifier negative-vector sweep (35/35 identical); `cargo test` (Rust verifier); ceremony rehearsal; PQ library ACVP/KAT vectors.
- **Toolchain freeze:** Go (verifier A), Node (verifier B), Rust (verifier C), Python (harness), libsodium (builder). Test = fiveam + run.sh + cargo test.
- **Migration:** policy epochs additive; hybrid AND; evidence-renewal chains (no retroactive invalidation). **Rollback:** new epoch reversible to prior signed epoch; verifier-per-epoch; PLANE-0 unchanged.
- **Evidence:** 35/35 error names with identically-recognized negative vectors; KW vectors `KILLS`; VS-12 pass; PQ ACVP/KAT logs; U-2 closed.
- **Paper dry-run (VS-12):** input = ceremony rehearsal (rotation, expired delegation, `key-compromise` with `invalid_from`, rollback checkpoint); expected = claim before `invalid_from` `VERIFIED`, between `retroactively-revoked`, after `revoked_at` `revoked`, rollback checkpoint `split-view`; failure path = verifier judging revocation against `valid_time` ⇒ KW-14 red; rollback = revert epoch.
- **Binary exit gate:** independent n-of-m ML-DSA (never "threshold ML-DSA") **AND** downgrade resistance (KW-104) **AND** 35/35 identical vectors across 3 verifiers **AND** U-2 closed. Else BLOCKED.
