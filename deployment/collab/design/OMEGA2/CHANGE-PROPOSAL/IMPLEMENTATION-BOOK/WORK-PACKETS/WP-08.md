# WP-08 — Neuro-symbolic reasoning + epistemic wall (Public Legal Discernment core)

**Bound `88129099` · EXECUTION NOT AUTHORIZED.** Entry gate = WP-03 + WP-07 exits.

- **Requirement:** R-129 (§4.17 formal Legal-IR semantic contract). MIS-1. closure-WP-04. (Exercises R-24..R-34 as a reasoning consumer; primary ownership WP-07/WP-03.)
- **Rationale:** This is the reasoning core of the **Public Legal Discernment Engine**: a candidate becomes Legal IR only through symbolic validation; determinacy answers are proof-carrying, and open-textured norms produce a typed `UNKNOWN(interpretive)` — honest abstention, never a guess.
- **Architecture seat:** S4 symbolic core. Frozen: `LAWMAX-LEGAL-IR-SEMANTIC-CONTRACT.md` (REQUIREMENTS spec).

### Elements
| element | path | package | disp | surface |
|---|---|---|---|---|
| inference / deontic | `source/legal-inference-engine.lisp`, `legal-deontic.lisp`, `legal-event-calculus.lisp` | `orchestrator.*` | KEEP/MODIFY | WFS evaluation, deontic operators |
| conflict / dialectic | `source/legal-conflict-resolution.lisp`, `legal-dialectic.lisp` | `orchestrator.*` | MODIFY | adopted scoped `ConflictPolicyBundle`; argument/counterargument |
| public subsumption | `source/legal-subsumption.lisp` | `orchestrator.subsumption` | MODIFY | **public reasoning only**; private-fact subsumption DEFER_PRIVATE |
| guard | `source/guard-metaeval.lisp` | `orchestrator.guard` | KEEP/MODIFY | meta-eval guard |
| write authority | `source/write-authority.lisp` | `orchestrator.write-authority` | KEEP | **single** journal write seat |

- **Contracts:** typed Legal IR (Fact/Norm/Claim/Proof); `UNKNOWN(interpretive){alternatives[](typed L5), open_objections[]}`; conflict = adopted, source-anchored, InstitutionalAct-approved `ConflictPolicyBundle` — absent ⇒ UNKNOWN, incompatible ⇒ CONFLICTING; **no invented universal `lex superior/specialis/posterior`**.
- **Deliverable 6 — Public Legal Discernment Engine (full synthesis, no new seat):** L5 hypothesis branching (`proposals.lisp`, WP-07) + Legal IR (`legal-ast.lisp`, WP-03) + symbolic/deontic/defeasible reasoning (this WP) + argument/counterargument (`legal-dialectic.lisp`) + L6 adversarial review (`review-service.lisp`, WP-12) + jurisprudence authority weighting (`citation-authority.lisp`, WP-09) + typed uncertainty/conflict (`USC §8`) + InstitutionalAct adoption (`reviewer_adoption_act`, WP-09) + proof-carrying answer (`proof-carrying.lisp`, WP-11). It is the **composition** of S3+S4+S6+S7+S9 — one seat each, orchestrated; no second engine.
- **Forbidden dependencies:** promotion candidate→IR only via symbolic validation (never neural score alone, I-4.3d); a PLANE-3 object in a release ⇒ compile failure (Q09).
- **Ordered edits:** (1) WFS + deontic evaluation over typed IR; (2) conflict evaluation via adopted scoped bundle (mechanism only, no substantive canon); (3) argument/counterargument with `open_objections`; (4) `UNKNOWN(interpretive)` for `determinacy: interpretive`; (5) ensure all journal writes funnel through the single `write-authority.lisp`.
- **Tests / kill tests:** Q09, Q31, Q32, Q33 · KW-7, KW-49, KW-50 · VS-06, VS-07.
- **Commands:** `sbcl` inference fiveam suite; release-with-PLANE-3-object ⇒ compile failure (Q09); `IN/OUT` for interpretive norm ⇒ KW-50.
- **Toolchain freeze:** SBCL; no external reasoning library on the trusted path (all reasoning is repo Lisp). **Decision gate:** none (mechanism frozen; substantive conflict canons are legal-review content, `PENDING_LEGAL_VALIDATION`, evidence cat [3]).
- **Migration:** conflict policy is adopted + source-anchored, forward-only. **Rollback:** new inference rules on a branch; revert with no journal effect.
- **Evidence:** PLANE-3 in release ⇒ compile failure; KW-7/49/50 `KILLS`; VS-06/07 pass.
- **Paper dry-run (VS-07):** input = ΑΚ art. 281 (κατάχρηση δικαιώματος, `determinacy: interpretive`), query "is right X barred?"; expected = `UNKNOWN(interpretive)` with typed alternatives (L5) + counterproof slot, passes verifier as `UNKNOWN(undecided-legal-state)`, no `IN/OUT`, no LegalRuleML emission; failure path = `IN/OUT` for interpretive ⇒ KW-50 red; rollback = revert rules.
- **Binary exit gate:** candidate→IR only via symbolic validation **AND** typed abstention (no guess) **AND** VS-06/07 pass. Else BLOCKED.
