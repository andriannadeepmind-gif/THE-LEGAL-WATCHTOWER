# WP-14 — Mission-scale qualification + provider adoption

**Bound `88129099` · EXECUTION NOT AUTHORIZED.** Entry gate = WP-13 exit; SPEC/IMPLEMENTATION/SECURITY QSR signed by independent auditors; explicit creator order to start the mission.

- **Requirement:** R-112..R-118 (qualification cross-cutting: 12-layer presence, audit, 5 grades, dominance, anti-loop, predeclared programme) + §5 (MISSION GREECE-1). MIS-9. Provider registry = §5 mission artifact (adoption format R-110 owned by WP-11).
- **Rationale:** Canonicity beyond SPEC is **earned**, not declared: `MISSION GREECE-1` over 30 days under independent auditors, every QSR with `expiry` and proven auto-downgrade, provider adoption signed by ≥2 registered providers.
- **Architecture seat:** cross-cutting qualification gate. Frozen: MLTP §3.1 QSR, Q-tests §2/§4/§8.

### Elements
| element | path | domain | disp | surface |
|---|---|---|---|---|
| QSR issuance | MLTP v3 §3.1 | protocol | KEEP | SPEC/IMPLEMENTATION/SECURITY/MISSION QSR with `expiry` |
| provider registry | **NEW** `LocalTrustState.provider_registry` | trust | NEW | registered providers, adoption attestations |

- **Contracts:** QSR `{level, subject, expiry, auditor_kid}`; `provider-adoption-qualified` (≥2 registered providers); Root Authority auto-downgrade on expiry (KW-13/25).
- **Forbidden dependencies:** no grade self-signed by the implementer (auditors only, MLTP §3.1); no "forever" QSR (R-115); mission abort ⇒ **no** canonicity.
- **Ordered edits:** (1) provider registry in `LocalTrustState`; (2) run the 15 vertical slices as regression; (3) run MISSION GREECE-1 (M-1..M-6, 30 days) under ≥2 auditors; (4) collect `provider-adoption-qualified` from ≥2 providers; (5) prove auto-downgrade at a live expiry.
- **Tests / kill tests:** Q27, Q28 + all Q01–Q42 live · KW-12, KW-13, KW-25, KW-46 · all 15 slices as regression.
- **Commands:** full `sbcl` test-system regression; `run.sh`; all VS bundles; 30-day mission telemetry; live-expiry downgrade proof.
- **Toolchain freeze:** all prior WP toolchains as regression. **Decision gate WP-14-a:** the independent auditor set (external, institutional — MLTP §3.1); this is a **hard external gate**, not a design decision.
- **Migration:** QSR with auto-downgrade at expiry, forward-only. **Rollback:** invalid QSR ⇒ automatic downgrade (KW-13/25); mission abort ⇒ no canonicity (safe default).
- **Evidence:** M-1..M-6 for 30/30 days with ≥2 auditor receipts; `provider-adoption-qualified` from ≥2 providers; every QSR with `expiry` + proven auto-downgrade.
- **Paper dry-run (all 15 VS as regression):** input = the frozen-derived release; expected = all 15 slices green as regression, all Q live, QSR auto-downgrade fires at a synthetic expiry; failure path = any abort condition ⇒ no canonicity; rollback = downgrade.
- **Binary exit gate:** independent auditors sign **AND** explicit creator order for MISSION **AND** no abort condition. Else BLOCKED (and canonicity is NOT claimed).
