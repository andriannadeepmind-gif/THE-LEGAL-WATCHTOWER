# WP-07 — Multimodal acquisition + external neural runtime + ontology-alignment plane

**Bound `88129099` · EXECUTION NOT AUTHORIZED.** Entry gate = WP-02 exit; U-6 defined.

- **Requirement:** R-24..R-30 (§4.3 neuro-symbolic plane), R-131 (§4.19 temporal ontology governance). MIS-1/MIS-7. closure-WP-02/03/17.
- **Rationale:** The neural runtime is **external, out-of-process, PLANE-3, non-authoritative**. Its only output is a `neural-candidate/1` that enters through the WP-02 non-evaluating decoder (never `safe-read`), and never self-promotes to VALIDATED (I-4.3a).
- **Architecture seat:** S2 (OCR/layout) + S3 (neuro-symbolic bridge, PLANE-3) + S17 (ontology governance). Frozen: v1.4 §4.2/§4.3/§4.4, MLTP §2.11.

### Elements
| element | path | package/domain | disp | surface |
|---|---|---|---|---|
| external decoder | `source/ingress-decoder.lisp` (WP-02) | `orchestrator.ingress` | KEEP | sole external decode seat; NOT `safe-read` |
| internal primitive | `source/safe-read.lisp` | `orchestrator.safe-read` | KEEP | INTERNAL-ONLY; untouched by external ingress |
| extraction verify | `source/legal-extraction-verify.lisp` | `orchestrator.extraction` | MODIFY | symbolic validation of candidates |
| ontology/graph | `source/greek-legislation-ontology.lisp`, `knowledge-graph.lisp`, `rdfs-inference.lisp`, `shacl-validator.lisp` | `orchestrator.*` | MODIFY | alignment + SHACL |
| L5 lifecycle | `source/proposals.lisp`, `anomaly-detection.lisp`, `fluid-induction.lisp` | `orchestrator.*` | MODIFY | hypothesis lifecycle |
| neural runtime | **NEW** `neural-runtime/` (external, out-of-process) | Python/PyTorch/ONNX | NEW | OCR/layout + ontology-mapping candidates |
| ontology bundle | **NEW** `ontology-bundle` + `shacl-validation-receipt` | contract | NEW | version-bound, shapes digest |

- **Contracts:** `neural-task/1{kind, runtime_manifest_sha256, input_ref}`; `neural-candidate/1{kind, anchors[], uncertainty, alternatives, candidate_id}`; `ontology-bundle{ontology_id, version, shapes_graph_digest}`; `shacl-validation-receipt`.
- **Forbidden dependencies:** neural runtime has **no write path** to L1 (F1); free field in the protocol ⇒ does not compile (negative build); a `candidate_id` is deterministic in `(input, runtime_manifest)`.
- **Ordered edits:** (1) define `neural-task/1`/`neural-candidate/1` closed protocol; (2) stand up the external out-of-process neural runtime (capability-less host, WP-02 sandbox); (3) OCR/layout candidate path → `ingress-envelope/1`; (4) ontology alignment candidates + SHACL; (5) `ontology-bundle` version binding + `shacl-validation-receipt`; (6) L5 hypothesis lifecycle.
- **Tests / kill tests:** Q04, Q30, Q31 · KW-49, KW-106 · VS-04, VS-05, VS-06(checkpoint).
- **Commands:** neural runtime container run (network-off assertion); `sbcl` extraction-verify fiveam suite; SHACL validation; two runs same input+manifest ⇒ same `candidate_id`.
- **Toolchain freeze:** neural = **Python + ONNX Runtime** (out-of-process, PLANE-3); ontology = **cxml/RDF** + SHACL (repo `shacl-validator.lisp`). **Decision gate WP-07-a (U-6):** held-out truth set + error threshold (U-1). **WP-07-b:** exact neural model/runtime pin (criteria: reproducible `runtime_manifest_sha256`, capability-less).
- **Migration:** ontology bundle versioned + shapes digest; revalidation ⇒ new receipt (no retroactive invalidation, KW-106). **Rollback:** neural runtime disableable (fail-closed: no candidate lane); ontology epoch reversible.
- **Evidence:** free protocol field ⇒ negative build; held-out error measured (U-1); VS-04/05 pass.
- **Paper dry-run (VS-04):** input = a corrupted historical scanned FEK page (rotated 90°, 1% bit-flip); expected = `UNKNOWN(ocr-below-threshold)` typed, never PLANE-1; same input+manifest ⇒ same `candidate_id`; failure path = corrupted scan promoted ⇒ Q30 red; rollback = disable runtime.
- **Binary exit gate:** no neural candidate self-promotes (I-4.3a) **AND** ontology version-bound **AND** VS-04/05 pass **AND** U-6 closed. Else BLOCKED.
