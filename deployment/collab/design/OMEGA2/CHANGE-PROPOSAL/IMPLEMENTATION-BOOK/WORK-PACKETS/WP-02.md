# WP-02 — Acquisition, provenance, authenticity + Secure Semantic Ingress sandbox

**Bound `88129099` · EXECUTION NOT AUTHORIZED.** Entry gate = WP-01 exit.

- **Requirement:** R-14..R-23 (§4.2 acquisition/authenticity), R-133 (§4.21 secure ingress). MIS-1. closure-WP-02.
- **Rationale:** This is the **external trust boundary**. External bytes must never become Lisp forms. The one place external input is decoded is a **new, non-evaluating JSON/CBOR decoder** — not `safe-read.lisp` (which stays internal-only, `cl:read`, never touching external bytes).
- **Architecture seat:** S2 Acquisition + Secure Semantic Ingress boundary. Frozen: v1.4 §4.2, `LAWMAX-SECURE-SEMANTIC-INGRESS-CONTRACT.md`.

### Elements
| element | path | package/domain | disp | surface |
|---|---|---|---|---|
| PDF authority | `source/pdf-authority.lisp` | `orchestrator.pdf` | KEEP | attested PDF path |
| layout/typography | `source/layout-types.lisp`, `source/validate-layout-graph.lisp`, `source/typographic-classifier.lisp`, `source/legal-ast.lisp` | `orchestrator.*` | MODIFY | tables/annex extraction |
| canonicalizer | `source/text-canonicalizer.lisp` | `orchestrator.canon` | KEEP | RFC 8785-style canonical text |
| provenance/receipts | `source/corpus-provenance.lisp`, `source/authority-proof-bundle.lisp`, `source/legal-authority-receipt.lisp`, `source/legal-identity.lisp`, `source/legal-id-registry.lisp` | `orchestrator.*` | MODIFY | USC 4-level ids, two-channel invariant |
| RFC-3161 / X.509 | `source/x509-authority.lisp`, `source/asn1-der.lisp`, `source/timestamp-authority.lisp` | `orchestrator.*` (CFFI-free ASN.1) | MODIFY | full RFC-3161 TSR, PAdES/XAdES detection |
| internal S-expr primitive | `source/safe-read.lisp` | `orchestrator.safe-read` | KEEP | **INTERNAL-ONLY**; `cl:read` on self-written data-only S-expr; **no external byte reaches it** |
| ingress sandbox host | **NEW** `source/ingress-sandbox.lisp` | `orchestrator.ingress` | NEW | out-of-process, capability-less (no fs/net/subprocess/clock-write) parser host |
| non-evaluating decoder | **NEW** `source/ingress-decoder.lisp` | `orchestrator.ingress` | NEW | `decode(ingress-envelope/1) → typed DTO` — **no `cl:read`/eval/macro/compile** |
| taint enforcer | **NEW** `source/taint-state.lisp` | `orchestrator.ingress` | NEW | `UNTRUSTED→PARSED→VALIDATED→ADOPTED→CANONICAL`, monotone; regress ⇒ `ingress-nonmonotonic-taint` |
| authority-proof/2, custody | **NEW** in `authority-proof-bundle.lisp` | — | NEW | custody chain + audiovisual manifestation |

- **Contracts:** `ingress-envelope/1` (RFC 8785 JCS / RFC 8949 CBOR); `parser-result/1`; `neural-candidate/1` (WP-07 only); `authority-proof/2`; USC `work_id/expression_id/manifestation_id/item_digest`.
- **Error taxonomy (Secure Ingress §6):** `reader-reached-external-bytes`, `non-canonical-ingress-envelope`, `schema-decode-failed`, `ingress-nonmonotonic-taint`, `ingress-grammar-violation`, `reader-macro-escape`, `package-symbol-escape`, `excessive-nesting`, `decompression-bomb`, `xml-entity-expansion`.
- **Forbidden dependencies:** the decoder/sandbox MUST NOT depend on `safe-read.lisp`, `cl:read`, `read-from-string`, or any reader-macro path (F2 in TARGET-DEPENDENCY-GRAPH).
- **Ordered edits:** (1) `ingress-sandbox.lisp` = capability-less out-of-process host; (2) sandboxed format parsers (PDF/XML/HTML/OCR/JSON/feed) → `ingress-envelope/1`; (3) `ingress-decoder.lisp` = non-evaluating envelope→DTO; (4) `taint-state.lisp` monotone enforcer; (5) extend provenance for `authority-proof/2` + custody chain; (6) extend `timestamp-authority.lisp` to full RFC-3161 TSR.
- **Tests / kill tests:** Q03, Q07, Q13, Q24, Q30 · KW-3/4/26/27/44/45 · **SIK-1..SIK-9** (frozen UNEXECUTED → executed here, each proving zero side effect) · VS-01(1–3), VS-03.
- **Commands:** `sbcl` fiveam ingress suite; SIK harness (sandbox, 0 capabilities, assert no fs/net/proc write); two-channel fixture → one `work_id`.
- **Toolchain freeze:** SBCL; CBOR/JSON = **jonathan/yason** (AS-IS) for **DTO shaping only, after** the non-evaluating decoder; XML = **cxml** with entity expansion disabled; compression bounds via **chipz** with output caps; ASN.1 = repo `asn1-der.lisp` (no homemade crypto — signatures verified via WP-06 vetted backends). **Decision gate WP-02-a:** sandbox isolation mechanism (criteria: capability-less, out-of-process, deterministic) — resolved before edit (1).
- **Migration:** new boundary seat; no mutation of existing records. **Rollback:** decoder/sandbox in their own modules; disabling ⇒ "no external ingestion" (fail-closed), never a fallback to external `cl:read`.
- **Evidence:** 100% sealed objects with 4 provenance elements; two-channel ⇒ one `work_id`; SIK-1..9 zero-side-effect harness logs; VS-03 bundle; VS-01 checkpoint.
- **Paper dry-run (VS-01 1–3, VS-03):** input = attested Constitution PDF (art. 4) + the same MAD from two official channels with a 1-char difference; expected = one `work_id`, two `expression_id`, `UNKNOWN(official-sources-conflict)` on both verifiers, and SIK `#.(delete-file "x")` on untrusted input ⇒ `read-time-execution-attempt` with 0 file/proc/net effect; failure path = any external byte reaching `cl:read` ⇒ `reader-reached-external-bytes` (structural) ⇒ no exit; rollback = disable decoder (fail-closed).
- **Binary exit gate:** no external byte in `cl:read`/eval **AND** SIK-1..9 pass (zero side effect) **AND** VS-03 pass. Else BLOCKED.
