# WP-12 — Website, cockpit, publication workflow + public→private enforcement

**Bound `88129099` · EXECUTION NOT AUTHORIZED.** Entry gate = WP-11 exit.

- **Requirement:** R-74..R-81 (§4.12 app/website/cockpit), R-111 (public→private, 9 absent types), R-91 ≡ R-79 (RBAC/MFA, same seat). §1.3/§1.4. MIS-6/MIS-10. closure-WP-12/13/18.
- **Rationale:** The cockpit emits **signed intent** into the M5 queue and can **never** direct-publish; the website is a **projection** of the same canonical release; the 9 private types are **structurally absent** (compile-time), not filtered at runtime.
- **Architecture seat:** S12 cockpit + S13 website + S18 public→private boundary. Frozen: v1.4 §4.12/§1.3/§1.4.

### Elements
| element | path | package/domain | disp | surface |
|---|---|---|---|---|
| cockpit | `source/cockpit.lisp` | `orchestrator.cockpit` | MODIFY; `/api/publish` **REPLACE** | `/api/publish` → approval intent to M5 (no release action) |
| review/queue | `source/http-server.lisp`, `review-service.lisp`, `review-queue.lisp`, `approval-policy.lisp`, `decisions.lisp`, `corpus-diff.lisp` | `orchestrator.*` | KEEP | M5 proposer-blind queue; L6 review |
| static site | `source/static-site.lisp` | `orchestrator.site` | KEEP | canonical URL `/lawmax/{path}` (one per Legal Object) |
| RBAC/MFA + app shell | **NEW** | `orchestrator.cockpit` | NEW | role registry, `mfa_evidence`, app shell |

- **Contracts:** `cockpit_intent{kind ∈ {proposal, approval}, actor_kid, role, mfa_evidence}` (closed); canonical URL `/lawmax/{path}`; the 9 private types absent from the public schema.
- **Forbidden dependencies (F4/F6):** cockpit/site MUST NOT write `release_root` or PLANE-0; direct-publish path MUST NOT exist (structural, VS-14); a matter-solving field ⇒ compile failure (KW-39); site view not reducible to a canonical release ⇒ red (R-77).
- **Ordered edits:** (1) REPLACE `/api/publish` with an intent producer (no release action); (2) RBAC/MFA role registry + `mfa_evidence`; (3) app shell (conversation-first); (4) enforce the 9 absent private types at compile time (schema); (5) site renders from the same canonical release digest.
- **Tests / kill tests:** Q15, Q17, Q20, Q39 · KW-38, KW-39, KW-57 · VS-14.
- **Commands:** `sbcl` cockpit fiveam suite; four bypass attempts (old `/api/publish`, intent without MFA, intent from kid outside registry, intent with `weakest_link`) ⇒ all structurally impossible; owner docker proof of Q15 (eight elements with evidence).
- **Toolchain freeze:** SBCL HTTP (`http-server.lisp`); app shell = static + conversation API. **Decision gate WP-12-a:** app-shell front-end stack (criteria: renders only proof-carrying answers + dual citation; no trust logic).
- **Migration:** `/api/publish` becomes an intent producer; 9 private types compile-time absent. **Rollback:** app shell versioned; intent queue reversible; never a direct-publish path.
- **Evidence:** VS-14 4/4 attempts structurally impossible; Q15 owner docker proof.
- **Paper dry-run (VS-14):** input = a source-correction proposal as `cockpit_intent kind=proposal`, then `kind=approval` by a `reviewer` with `mfa_evidence`; expected = `RELEASED` only via M5, all four bypass attempts fail **structurally** (no path / no compile / `untrusted-key`); failure path = direct-publish bypass succeeds ⇒ Q15(β) red; rollback = intent queue reset.
- **Binary exit gate:** 1 release via M5 **AND** direct-publish structurally impossible **AND** private type ⇒ compile failure (Q20). Else BLOCKED.
