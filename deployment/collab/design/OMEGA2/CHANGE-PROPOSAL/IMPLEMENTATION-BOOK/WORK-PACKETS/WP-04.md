# WP-04 — First deterministic Legal Compiler (Common Lisp, domain A)

**Bound `88129099` · EXECUTION NOT AUTHORIZED.** Entry gate = WP-03 exit.

- **Requirement:** R-40 (independently produced legal-state root), R-43 (no compiler certifies itself). §4.6. MIS-5. closure-WP-08. (R-39/R-41/R-42 complete at WP-05 — DUAL; no requirement implemented twice.)
- **Rationale:** From the journal, compiler A produces `legal_state_root` + `projection_roots` reproducibly, with proposer-blind M5 verification over the new root. A is one of two **required-distinct** implementations.
- **Architecture seat:** S8a (compiler A domain). Frozen: v1.4 §4.6, MLTP §13.4, `LAWMAX-LEGAL-IR-SEMANTIC-CONTRACT.md` §10.

### Elements
| element | path | package | disp | surface |
|---|---|---|---|---|
| compiler A | `source/consolidation-engine.lisp`, `source/consolidation-proof.lisp`, `source/version-graph.lisp`, `source/legal-inference-engine.lisp` | `orchestrator.consolidation` | KEEP/MODIFY | journal → `legal_state_root_A` + `projection_roots_A` |
| release/M5 | `source/release-authority.lisp`, `source/release-gate.lisp`, `source/verify-truth-gate.lisp` | `orchestrator.release` | MODIFY | proposer-blind M5 over the new root |
| third check | `deployment/verify/verify-temporal.py` | python | KEEP | independent temporal cross-check |

- **Contracts:** `legal_state_root_A` (hash); `projection_roots_A{consolidated_text, in_force_set, citation_graph, line_of_authority}`; `compiler-attestation{kid_A, root, scope:"compiler-attestation"}`.
- **Domain A isolation (deliverable 7):** package domain `orchestrator.consolidation`; build image **A** (SBCL, `deps.lock`-A); signing key **kid_A** (delegated, scope compiler-attestation). **No shared evaluator, image, lock, or key with domain B.**
- **Forbidden dependencies:** `S8a` MUST NOT depend on `S8b` (F3); the agreement A=B is verified at `S8g` (WP-05), never used as an admission predicate inside A (Q11 β).
- **Ordered edits:** (1) make `consolidation-engine.lisp` produce a canonical `legal_state_root_A` from the journal deterministically; (2) emit `projection_roots_A`; (3) wire proposer-blind M5 in `release-gate.lisp` over the new root; (4) emit `compiler-attestation` with delegated `kid_A`.
- **Tests / kill tests:** Q11, Q12 · VS-01(7), VS-02(5), VS-09(A).
- **Commands:** `sbcl --load orchestrator.asd --eval '(orchestrator.consolidation:compile-legal-state <journal>)'` twice → compare roots; `python3 deployment/verify/verify-temporal.py`.
- **Toolchain freeze:** Common Lisp = **SBCL** (domain A only); hashing = **ironclad** SHA-256; canonical serialization = `canonical-representation.lisp`. Image A + `deps.lock`-A. **Decision gate:** none.
- **Migration:** reproducible root from journal, forward-only. **Rollback:** new root on its own branch; reject ⇒ restore prior signed root (PLANE-0 unchanged).
- **Evidence:** two runs ⇒ identical `legal_state_root_A` (Q12); `compiler-attestation` with `kid_A`; VS checkpoints.
- **Paper dry-run (VS-01 step 7):** input = journal of VS-01/VS-02; expected = identical `legal_state_root_A` on two runs, `compiler-attestation` signed by `kid_A`; failure path = non-deterministic root ⇒ Q12 red ⇒ no exit; rollback = restore prior root.
- **Binary exit gate:** root determinism 2/2 **AND** proposer-blind M5 (agreement not used as admission predicate). Else BLOCKED.
