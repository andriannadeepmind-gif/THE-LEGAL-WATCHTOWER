# WP-05 — Second independent compiler (Rust, domain B) + differential verification

**Bound `88129099` · EXECUTION NOT AUTHORIZED.** Entry gate = WP-04 exit; U-5 decided.

- **Requirement:** R-39 (separate implementations/runtimes), R-41 (compare roots + critical projections), R-42 (automatic quarantine on divergence). §4.6. MIS-5. closure-WP-08.
- **Rationale:** The dual requirement is satisfied only when a **second, independent** compiler exists and a comparison/quarantine gate blocks any single-attestation release. Differential verification is the content of §4.6, not a duplicate seat.
- **Architecture seat:** S8b (compiler B domain) + S8g (comparison/quarantine gate). Frozen: v1.4 §4.6, MLTP §6 `dual_compiler_attestation`, §8.3 R4.

### Elements
| element | path | domain | disp | surface |
|---|---|---|---|---|
| compiler B | **NEW** `compilers/legal-compiler-b/` (Rust crate) | Rust crate `legal-compiler-b` | NEW | journal → `legal_state_root_B` + `projection_roots_B` |
| differential harness | **NEW** `deployment/verify/differential/` | Rust/Python | NEW | compare A vs B before signing |
| quarantine gate | `source/release-gate.lisp` | `orchestrator.release` | MODIFY | `QUARANTINED` path on divergence |

- **Contracts:** `legal_state_root_B`; `dual_compiler_attestation{kid_A, kid_B, root_A, root_B, equal}`; divergence ⇒ `compiler-divergence` (R4) ⇒ whole bundle `UNVERIFIED_FOR_MACHINE_RELIANCE`.
- **Domain B isolation (deliverable 7 — hard):** **Rust** toolchain; crate `legal-compiler-b` with its **own** `Cargo.lock`; build image **B** (Rust, distinct from image A); signing key **kid_B** (distinct delegated key). **Zero shared evaluator code, build image, dependency lock, or signing key with domain A.** They meet only at `S8g` (comparison), which reads A's and B's roots as opaque bytes.
- **Forbidden dependencies:** `S8b` MUST NOT depend on `S8a` code, its SBCL image, its `deps.lock`, or `kid_A` (F3, enforced by `target-depgraph-check.py`).
- **Ordered edits:** (1) implement compiler B in Rust reading the same journal, emitting `legal_state_root_B` by the same *spec* (not the same *code*); (2) build the differential harness comparing roots + critical projections **before** signing; (3) `release-gate.lisp` `QUARANTINED` path when `root_A ≠ root_B`; (4) emit `dual_compiler_attestation` with two distinct `kid`.
- **Tests / kill tests:** Q33, Q34 · KW-52 (single-attestation release ⇒ red) · VS-09 (equal roots), VS-10 (divergence ⇒ quarantine).
- **Commands:** `cargo test` (crate B); `cargo build --release` (image B); differential harness over VS-01/02 journal; inject the declared error (`<=` vs `<`) ⇒ assert quarantine.
- **Toolchain freeze:** **Rust** (compiler B). **Decision gate WP-05-a (U-5):** exact Rust edition + minimal crate set — criteria: no crate shared with domain A's semantics, own `Cargo.lock`, reproducible build; resolved before edit (1). Hashing in B = a maintained Rust SHA-256 crate (RustCrypto), distinct from A's ironclad; **no homemade crypto.**
- **Migration:** additive second compiler; no type change. **Rollback:** compiler B crate removable; without B, **no release** (fail-closed single-attestation block), never a single-attestation release.
- **Evidence:** A=B root equality (VS-09); injected error ⇒ quarantine 1/1 (VS-10); 0 releases with one attestation.
- **Paper dry-run (VS-09/VS-10):** input = VS-01/02 journal (+ injected `<=`/`<` boundary error for VS-10); expected VS-09 = `root_A == root_B`, two attestations with distinct `kid`; expected VS-10 = `root_A ≠ root_B` ⇒ `compiler-divergence` ⇒ `QUARANTINED`, 0 releases; failure path = release picking A's root ⇒ red; rollback = quarantine journaled, no release.
- **Binary exit gate:** VS-09 + VS-10 pass **AND** 0 single-attestation releases **AND** `target-depgraph-check.py` confirms A/B isolation. Else BLOCKED.
