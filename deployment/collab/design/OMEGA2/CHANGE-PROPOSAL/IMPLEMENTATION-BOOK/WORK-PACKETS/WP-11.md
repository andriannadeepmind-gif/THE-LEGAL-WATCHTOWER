# WP-11 — Proof-carrying query API / MCP / SDK + Citation-Bound Verification Profile

**Bound `88129099` · EXECUTION NOT AUTHORIZED.** Entry gate = WP-05 + WP-06 + WP-10 exits.

- **Requirement:** R-44..R-47 (§4.7 proof-carrying query), R-71..R-73 (§4.11 standards), R-101..R-110 (§4.15 provider integration), R-119..R-124 (§4.16 citation-bound). MIS-5/MIS-6. closure-WP-09/14.
- **Rationale:** Every answer is proof-carrying or a typed `UNKNOWN` (never a guess), and every result binds its citation (`citation-unbound` ⇒ `UNVERIFIED_FOR_ATTRIBUTED_RELIANCE`), so a provider cannot strip attribution and still claim verification.
- **Architecture seat:** S9 proof-carrying query + S14 API/MCP/SDK. Frozen: v1.4 §4.7/§4.11/§4.15/§4.16, MLTP §2.10.

### Elements
| element | path | package/domain | disp | surface |
|---|---|---|---|---|
| query engine | `source/legal-qa.lisp`, `legal-reasoning-bridge.lisp`, `proof-carrying.lisp`, `legal-dialectic.lisp` | `orchestrator.*` | KEEP/MODIFY | `proof-carrying-answer/1` |
| MCP | `source/mcp-server.lisp` | `orchestrator.mcp` | MODIFY | mandatory `citation` on every tool result |
| URIs/emit | `source/canonical-uris.lisp`, `json-emit.lisp`, `akoma-ntoso-emitter.lisp`, `sparql-endpoint.lisp`, `corpus-sparql.lisp` | `orchestrator.*` | KEEP/MODIFY | canonical citation URLs, JSON-LD |
| feeds | `source/ai-corpus-dump.lisp`, `ai-ingest-manifest.lisp` | `orchestrator.*` | MODIFY | signed delta feeds |
| answer type / OpenAPI / SDK | **NEW** | multi | NEW | `proof-carrying-answer/1`, OpenAPI 3.x, SDKs (Py/TS/Rust), LegalRuleML emitter, `/audit/{claim_id}`, conformance suite |

- **Contracts:** `proof-carrying-answer/1` (16 fields, §4.7); `CertifiedResult{result_id, citation/1, citation_digest}`; `CitationToken`; `min` result-sum ordering.
- **Forbidden dependencies:** a schema with an **optional** `citation` ⇒ conformance FAIL (Q42); a default answer carrying `acquired_at` ⇒ Q41(γ) red; SDK carrying trust logic ⇒ Q22 red (thin SDK only).
- **Ordered edits:** (1) `proof-carrying-answer/1` type; (2) OpenAPI 3.x over `capability-api`; (3) versioned MCP with mandatory `citation`; (4) thin SDKs (Py/TS/Rust) defaulting to dual-citation rendering; (5) LegalRuleML emitter (mechanical only); (6) `/audit/{claim_id}`; (7) conformance suite; (8) signed delta feeds.
- **Tests / kill tests:** Q14, Q27, Q35, Q38, Q41, Q42 · KW-53/56/62/63 · VS-01(completion), VS-11.
- **Commands:** OpenAPI validation; MCP tool list; three-verifier conformance vectors (i–v of VS-11); SHACL 0 violations on published RDF; `cargo test` (Rust SDK).
- **Toolchain freeze:** OpenAPI 3.x; JSON = jonathan/yason; RDF = cxml; SDKs Python/TypeScript/Rust (thin wrappers over the verifiers). **Decision gate WP-11-a:** SDK packaging/versioning policy (criteria: LOC-ceiling, no trust logic).
- **Migration:** versioned OpenAPI/MCP; optional `citation` ⇒ conformance FAIL. **Rollback:** API version backward-compatible; SDK versioned.
- **Evidence:** 18/18 VS-11 receipts; default answer without `acquired_at`; SHACL 0 violations; stripped citation ⇒ `UNVERIFIED_FOR_ATTRIBUTED_RELIANCE`.
- **Paper dry-run (VS-11):** input = VS-01 `TrustBundle` + `CertifiedResult` + conformance vectors (i stripped citation, ii altered `official_source_uri`, iii expired QSR, iv `LocalTrustState` without trusted time, v `CitationToken` on another `result_id`); expected = 3/3 `VERIFIED` positive, (i) ⇒ `citation-unbound` ⇒ `UNVERIFIED_FOR_ATTRIBUTED_RELIANCE`, bundle result = `min`; failure path = verifier giving `VERIFIED` on (i) ⇒ KW-62; rollback = revert API version.
- **Binary exit gate:** proof-carrying or `UNKNOWN` (never guess) **AND** citation-bound (KW-62/63) **AND** VS-01/VS-11 complete. Else BLOCKED.
