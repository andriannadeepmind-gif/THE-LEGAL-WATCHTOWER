# WP-13 — Citation observatory + security/operational observatory

**Bound `88129099` · EXECUTION NOT AUTHORIZED.** Entry gate = WP-11 + WP-12 exits.

- **Requirement:** R-82..R-84 (§4.13 citation observatory), R-93/R-95/R-96/R-97/R-98 (§4.14 vuln/DR/multi-region/incident/SLO). MIS-9. closure-WP-15.
- **Rationale:** Metrics are never legal correctness, and a collector reports **real numbers or a typed `UNKNOWN`** — never a stub that silently reports "0". Disaster recovery reproduces the same qualified release byte-for-byte or declares typed loss.
- **Architecture seat:** S15 observatories. Frozen: v1.4 §4.13/§4.14/§4.16.

### Elements
| element | path | package/domain | disp | surface |
|---|---|---|---|---|
| citation collectors | `source/ai-citation-strategy.lisp`, `citation-authority.lisp` | `orchestrator.*` | MODIFY | real collection or typed UNKNOWN |
| metrics config | `configs/prometheus-citation.yml`, `deployment/templates/ai-citation-log.ttl` | config | KEEP | metric streams |
| resilience | `source/circuit-breaker.lisp`, `logging.lisp` | `orchestrator.*` | KEEP | breakers, structured logs |
| threat model | `deployment/LAWMAX-THREAT-MODEL.md` | doc (NORMATIVE, frozen) | reference | Θ3/4/5/9/10 closed in implementation |
| new monitors | **NEW** | `orchestrator.observatory` | NEW | revoked-material detector, provider-compliance monitor, SLO registry, vulnerability monitoring, DR runbook, incident feed |

- **Contracts:** SLO registry (measured numbers, U-1); public incident feed; split-view drill; `coverage_state`/metric ∈ {value | `UNKNOWN`}.
- **Forbidden dependencies:** a collector reporting "0" while a stub ⇒ red (KW-58); observatory is **read-only** over other subsystems (S15 metric edges only, never a write path).
- **Ordered edits:** (1) revoked-material detector; (2) provider compliance monitor; (3) SLO registry (measured); (4) vulnerability monitoring (SBOM scan); (5) DR runbook + incident feed; (6) split-view drill.
- **Tests / kill tests:** Q16, Q19, Q40, Q42 · KW-58, KW-59, KW-63 · VS-15.
- **Commands:** SBOM scan; DR cold-rebuild from PLANE-0 + journal ⇒ same `release_root`; split-view drill; stripped-citation detection in a controlled scenario.
- **Toolchain freeze:** Prometheus (metrics, existing config); SBOM scanner (from WP-00). **Decision gate WP-13-a (U-1):** final RTO/RPO numbers from measurement.
- **Migration:** collectors or typed UNKNOWN (never stub "0"); DR runbook. **Rollback:** collector modules removable; UNKNOWN instead of silence.
- **Evidence:** VS-15 (R equal, RTO/RPO measured); 0 stub collectors; split-view drill 1/1; stripped-citation detected 1/1.
- **Paper dry-run (VS-15):** input = the VS-09/VS-11 release + a cold container with only PLANE-0 bytes + journal; expected = byte-identical `release_root`, QSR still resolve; second scenario = partial PLANE-0 loss ⇒ typed loss list ⇒ `UNKNOWN`, never cache-fill; failure path = loss "filled" from cache ⇒ Q19 red; rollback = declare loss typed.
- **Binary exit gate:** no silent zero metric **AND** DR reproduces byte-identical release **AND** U-1 final numbers. Else BLOCKED.
