# WP-09 — Full jurisprudence-evolution plane

**Bound `88129099` · EXECUTION NOT AUTHORIZED.** Entry gate = WP-03 + WP-08 exits; U-3 closed.

- **Requirement:** R-51..R-56 (§4.9 jurisprudence evolution). MIS-4. closure-WP-07.
- **Rationale:** Four separate classes of jurisprudential object, a bitemporal line-of-authority graph with measured `authority_weight`, and — critically — AI **never** produces an institutional ratio automatically: adoption requires a `reviewer_adoption_act` (KW-36).
- **Architecture seat:** S7 jurisprudence plane. Frozen: v1.4 §4.9, MLTP §2.5/§2.6/§8.3-J.

### Elements
| element | path | package | disp | surface |
|---|---|---|---|---|
| decisions/citations | `source/legal-decisions.lisp`, `decisions.lisp`, `citation-authority.lisp` | `orchestrator.*` | MODIFY | four classes, explicit-citation relations |
| judge | `source/jurisprudence-judge.lisp` | `orchestrator.jurisprudence` | KEEP | class assignment |
| line-of-authority | `source/version-graph.lisp` | `orchestrator.version-graph` | MODIFY | bitemporal line-of-authority graph |
| private (untouched) | `source/legal-precedent.lisp`, `legal-casegrammar.lisp` | — | DEFER_PRIVATE | not touched |
| ECLI / reviewer registry | **NEW** | `orchestrator.jurisprudence` | NEW | ECLI impl, reviewer registry + adoption act, line-of-authority graph |

- **Contracts:** `judgment-identity-and-text{ecli|provisional_id, anonymization_state}`; `jurisprudential-analysis{reviewer_adoption_act(kid)}`; `LATER-TREATMENT` (7-value vocabulary, R-53; incl. followed, distinguished, overruled); `authority_weight` (plenary > chamber).
- **Forbidden dependencies:** `later_treatment` without a citation anchor ⇒ KW-55; a ratio candidate without adoption in a release ⇒ `unadopted-analysis` (KW-7/36); a party name in a hash-bearing object ⇒ Q25 red.
- **Ordered edits:** (1) implement ECLI + `provisional_id`; (2) `rel1:` records from explicit citations with passage anchors; (3) `LATER-TREATMENT` events; (4) line-of-authority graph + `authority_weight`; (5) reviewer registry + `reviewer_adoption_act`; (6) class-4 ratio candidates kept **out** of release.
- **Tests / kill tests:** Q07, Q08, Q25, Q37 · KW-3/7/36/44/55 · VS-08.
- **Commands:** `sbcl` jurisprudence fiveam suite; three-Areios-Pagos-decision fixture (followed/overruled); anonymization typed 100%.
- **Toolchain freeze:** SBCL; ECLI/ELI = repo emitters (cxml/RDF). **Decision gate WP-09-a (U-3, external):** licensing for doctrine/full-text and the anonymization policy — closes at WP-09 entry.
- **Migration:** class-3 analysis only via adoption act, forward-only. **Rollback:** reviewer registry versioned; revert adoption = `REVERT` event.
- **Evidence:** 0 `later_treatment` without anchor; 0 ratio without adoption in release; anonymization typed 100%; VS-08 pass.
- **Paper dry-run (VS-08):** input = three Areios Pagos decisions (2nd explicitly follows 1st; 3rd plenary explicitly overrules); expected = one `work_id` per decision, graph with `overruled` edge, plenary weight > chamber, class-4 ratio candidates unseen by verifier; failure path = ratio candidate without adoption ⇒ `unadopted-analysis`; rollback = `REVERT`.
- **Binary exit gate:** never ratio as institutional fact without adoption (KW-36) **AND** U-3 closed **AND** VS-08 pass. Else BLOCKED.
