# WP-03 — Typed Legal IR + bitemporal event store

**Bound `88129099` · EXECUTION NOT AUTHORIZED.** Entry gate = WP-02 exit.

- **Requirement:** R-31..R-34 (§4.4 language/runtime boundary), R-35..R-38 (§4.5 bitemporal twin). MIS-1/MIS-3. closure-WP-04/05. (§4.3 R-24..R-30 owned by WP-07; consumed here as IR input.)
- **Rationale:** A closed catalog of 15 event kinds with `valid × known` separation, `legal-timeline/1` in the payload and `audit-timeline/1` in the proof, makes "knowledge time never judges legal validity" a type-level fact (KW-60), and every event traceable to a source manifestation (KW-51).
- **Architecture seat:** S4 symbolic core + S5 bitemporal event store (L1/L2). Frozen: v1.4 §4.5, MLTP §2.0, `LAWMAX-TEMPORAL-SEMANTICS-SPEC.md`.

### Elements
| element | path | package | disp | surface |
|---|---|---|---|---|
| version graph | `source/version-graph.lisp` | `orchestrator.version-graph` | MODIFY | judicial/EU events, `known_from` per event |
| temporal / event calculus | `source/legal-temporal.lisp`, `source/legal-event-calculus.lisp`, `source/journal.lisp` | `orchestrator.*` | KEEP | append-only L1 |
| EU links | `source/corpus-eu-links.lisp`, `source/eu-interop-layer.lisp` | `orchestrator.*` | MODIFY | ELI/EU event linkage |
| decisions/citations | `source/citation-authority.lisp`, `source/legal-decisions.lisp` | `orchestrator.*` | MODIFY | LATER-TREATMENT from explicit citations |
| Legal AST | `source/legal-ast.lisp` | `orchestrator.legal-ast` | MODIFY | `norm.determinacy` type |
| timelines | **NEW** `legal-timeline/1`, `audit-timeline/1` types | `orchestrator.legal-ast` | NEW | payload vs proof separation |

- **Contracts:** 15 closed event kinds (incl. `ENACTMENT, AMENDMENT, COMMENCEMENT, REPEAL, CROSS-REFERENCE, UNCERTAINTY, LATER-TREATMENT`); `legal-timeline/1{issued_at, published_at, effective_from, effective_to, ceased_by, cessation_type}`; `audit-timeline/1{acquired_at, verified_at, known_from}`; `norm.determinacy ∈ {determinate, interpretive}`.
- **Forbidden dependencies:** event store (S5) MUST NOT derive from neural (S3) directly (F1); a payload carrying an audit field ⇒ `malformed-envelope` (KW-61).
- **Ordered edits:** (1) define the 15 closed event kinds; (2) add `legal-timeline/1`/`audit-timeline/1` types with the payload/proof split; (3) add `norm.determinacy`; (4) extend `version-graph.lisp` for judicial/EU events + `known_from`; (5) admission rule: every event carries a `manifestation_id` (KW-51).
- **Tests / kill tests:** Q05, Q06, Q08, Q41 · KW-51, KW-60, KW-61 · VS-01(4–6), VS-02(1–4), VS-07(IR). KT5 (`TPKill`) executed with ≥1 killed witness.
- **Commands:** `sbcl` fiveam temporal suite; `TPKill` TLA+ model executed; envelope-with-audit-field ⇒ `malformed-envelope`.
- **Toolchain freeze:** SBCL; time = **local-time**; model checking = **TLA+/TLC** (`TPKill`, already staged in `formal-v1.1/`). **Decision gate:** none (all seats existing + closed new types).
- **Migration:** additive event kinds, forward-only; `known_from > k` events excluded from an as-known-at-`k` projection. **Rollback:** new kinds backward-compatible; revert = append `REVERT` event, never delete journal.
- **Evidence:** every event with source proof (KW-51); `TPKill` executed log; VS-01/02/07 checkpoints.
- **Paper dry-run (VS-02 1–4):** input = Ν.5221/2025 amendments (effective 1/1/2026) + transitional provision; expected = `AMENDMENT`+`COMMENCEMENT`(future `effective_from`)+`CROSS-REFERENCE`+`UNCERTAINTY(transitional)`; the `(v=2026-01-01, k=2025-12-01)` cut shows text as known then (no `known_from>k` events); failure path = an event without `manifestation_id` ⇒ admission reject (KW-51); rollback = `REVERT` event.
- **Binary exit gate:** knowledge time never judges legal validity (I5) **AND** KT5 executed **AND** VS-01/02/07 checkpoints pass. Else BLOCKED.
