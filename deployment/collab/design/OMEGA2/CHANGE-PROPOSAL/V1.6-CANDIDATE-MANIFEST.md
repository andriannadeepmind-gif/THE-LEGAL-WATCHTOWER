# V1.6-CANDIDATE-MANIFEST — LAWMAX OMEGA (CURRENT CANDIDATE · NOT FROZEN)

**ΚΑΤΑΣΤΑΣΗ: `SPEC v1.6 CURRENT CANDIDATE — NOT FROZEN — NOT QUALIFIED — IMPLEMENTATION BLOCKED`.**

```
PARENT_COMMIT        = 112379cc   ([0150] v1.5 finite adversarial-repair)
FROZEN_v1.4_BASELINE = 88129099be1ad69feb80d40337ede6c286b83223   (UNCHANGED · not amended/rewritten)
SUCCESSOR_OF         = CHANGE-PROPOSAL-v1.5.md   (same CPEI Public Observatory Profile v1.4)
SCOPE                = future-extensibility + public cognition + memory + model independence + registries
                       (one bounded integration; no new/second/third architecture)
SOURCE_OF_TRUTH      = V1.6-SCHEMAS.sexp + SUBSYSTEM-REGISTRY.sexp + INTERFACE-AND-SCHEMA-REGISTRY.sexp
TARGET               = LAWMAX OMEGA — THE LEGAL WATCHTOWER OF GREECE
```
Το frozen v1.4 (`88129099`) παραμένει αμετάβλητο ιστορικό. Το Implementation Book (v1.0+v1.1) **δεν** αλλάζει.
Το v1.6 είναι **CANDIDATE** — δεν παγώνει τίποτα, δεν διεκδικεί qualification.

## 1. Τι ενσωματώθηκε
Όλες οι έγκυρες v1.5 διορθώσεις (D1/D2/D3/C1 + F1–F5 + R1–R8/A-1..D-1/F7) παραμένουν κανονικές· ο v1.6 audit
τις τρέχει ως regression. Προστέθηκαν: model-agnostic `SemanticProposer` (ONNX optional), Public Language
Cognition Layer (μία έδρα), πλήρης Memory Architecture (μία έδρα), 13 σταθερά contracts, extension contracts
(private/real-time/embodiment, interfaces only), adapter replaceability, δύο machine-readable registries.

## 2. ΝΕΑ ARTIFACTS (v1.6 candidate)
| SHA-256 | file | classification |
|---|---|---|
| `000e7dceb505b008b49c476c548865b718cb6aa35d0cc460cc229bb8687e410b` | `CHANGE-PROPOSAL-v1.6.md` | NORMATIVE |
| `4e8eaf6bc1c6a2b7b10e4bee39472c5ee2a06d6638658a09817639ee3f60fd08` | `V1.6-SCHEMAS.sexp` | NORMATIVE |
| `6d4fea2816699a66257d2e3b741a42ede6ccf94198f2280f62084a6d5b16ca17` | `SUBSYSTEM-REGISTRY.sexp` | NORMATIVE (source of truth · v1.7-extended: +S25/S26) |
| `bb40f8a048ee07ee6f55f183f438a7aee1cd90e013ffc034c6694782f2fc377b` | `INTERFACE-AND-SCHEMA-REGISTRY.sexp` | NORMATIVE (source of truth · v1.7-extended: +RA interfaces · v1.8-extended: +19 v1.8 contracts for seat closure) |
| `10b07386c17598e862bc4b6cd40eee2272e2d7d6f7bff70da322e40d723856d2` | `IMPLEMENTATION-BOOK-MIGRATION-IMPACT-v1.6.md` | INFORMATIVE |
| (self) | `V1.6-CANDIDATE-MANIFEST.md` | NORMATIVE |
| (script) | `V1.6-CONTRADICTION-OMISSION-AUDIT.sh` | GENERATED/EVIDENCE |

## 3. ΕΠΕΚΤΑΘΗΚΑΝ (additive v1.6 sections σε υπάρχουσες canonical έδρες — μία πηγή, όχι δεύτερη)
`PUBLIC-OBSERVATORY-CROSSWALK.md` (ONNX→optional adapter, SemanticProposer, cognition, memory seats),
`TRACEABILITY-MATRIX.md` (§v1.6 R-V6-* rows), `PUBLIC-OBSERVATORY-QUALIFICATION-TESTS.md` (§v1.6 V6Q/V6KW),
`LAWMAX-THREAT-MODEL.md` (Θ20 proposer/adapter substitution & memory-scope leakage),
`SUPERSEDED-REGISTER.md` (v1.6 note: «neural plane» framing → SemanticProposer),
`LAWMAX-SECURE-SEMANTIC-INGRESS-CONTRACT.md` (SemanticProposer non-evaluating ingress note).

## 4. ΔΕΝ ΑΓΓΙΧΤΗΚΑΝ (verified)
`source/` · `systems/` · `.github/` · `deployment/verify/mltp3/` · `RAW-JOURNAL-PARTIAL.jsonl` ·
`deployment/self/history.sexp` · `output/.healthy` · `IMPLEMENTATION-BOOK/` (v1.0+v1.1 bound to `88129099`) ·
ο pinned v1.4 audit `.out` (`4873e610`) · το frozen `CHANGE-PROPOSAL-v1.4.md`. Καμία production/source αλλαγή,
κανένα file move, κανένα WP-00, κανένα freeze.

## 5. MEMORY COVERAGE (`EXISTING | PARTIAL | MISSING`) — rebuilt from ACTUAL code (file:line:symbol)
Every disposition below is grounded in a real defining symbol in `source/`, not asserted. Knowledge/ontology
is a **SEPARATE seat** from semantic memory (see the note) — the prior table wrongly conflated them, and
wrongly marked PROSPECTIVE_GOALS/EPISODIC as MISSING/PARTIAL when they EXIST.

| PublicMemoryType | seat (file:line · symbol) | status |
|---|---|---|
| WORKING_CONTEXT | `memory.lisp:48 *session*` + `cognition.lisp:42/46/50 working-memory / remember / recall` | EXISTING (per-process); PARTIAL as durable multi-turn store |
| EPISODIC_INTERACTION | `memory.lisp:79 record-episode` · `:113 episodes` · `:128 recent-episodes` · `:133 find-episodes` | **EXISTING** (append-only SHA-256-chained stream) |
| SEMANTIC | `memory.lisp:72 %lemma-set` · `:70 episode-lemmas` (lemma index over episodes) | **PARTIAL** — linguistic recall only; NOT the knowledge/ontology seat (see note) |
| PROCEDURAL | `legal-inference-engine.lisp:238 make-jtms` · `legal-deontic.lisp:169 evaluate-deontic` · `rdfs-inference.lisp:386 apply-rdfs-rules` · `legal-event-calculus.lisp:43 ec-initiation` | PARTIAL — reasoning engines exist; no persisted procedural-memory store |
| PROSPECTIVE_GOALS | `memory.lisp:203 record-goal` · `:226 open-goals` · `:207 update-goal` · `:247 arm-intention` · `:258 fire-due-intentions` (`:goal`/`:intention`) | **EXISTING** (was wrongly MISSING) — goal agenda + armed intentions |
| SOURCE_PROVENANCE | `memory.lisp:150 %episode-payload` · `:157 %episode-hash` · `:167 verify-episode-chain` | **EXISTING** (content-addressed + chain-verified) |
| TEMPORAL | `memory.lisp:68 episode-at` (mono-temporal) vs `version-graph.lisp:227 tv-valid-from/tv-recorded-from` (bitemporal, SEPARATE seat) | PARTIAL — bitemporal exists only for legal-text versions, not the episode stream |
| ARGUMENT_COUNTERARGUMENT | `legal-dialectic.lisp:20 dialectic-report` (Dung grounded semantics) | **EXISTING** |
| UNCERTAINTY_CONTRADICTION | `memory.lisp:167 verify-episode-chain` cause set {`:missing-hash`, `:chain-break`, `:hash-mismatch`, `:unserializable-payload`} + `legal-dialectic` undecided + `lawmax/uncertainty/1` (v1.4) | PARTIAL — detection at reasoning layer; no dedicated over-time memory store |
| USER_PREFERENCE | (no `preference`/`user-pref` defining form in `source/`) | **MISSING** |
| SKILL_CAPABILITY | `capability-registry.lisp:40 capability` · `:104 register-capability` | PARTIAL — static tool/API registry, NOT a learned-skill memory |
| META_MEMORY | `memory.lisp:167 verify-episode-chain` (checked-count n + first fault) consumed by `self-model.lisp:304` | PARTIAL (was wrongly MISSING) |

**Private (deferred):** `PRIVATE_CLIENT_MATTER` → `PrivateMemoryEvent/1` + `PrivateMatterProfile/1` (§5 extension,
`:public-dependency nil`). NOT in the public base type/scope (defect 4).

**Knowledge / ontology is a SEPARATE seat, NOT semantic memory** (memory.lisp:6 — "memory holds what the system
*lived*; the graph/ontology hold what it *knows*"): `legal-knowledge.lisp:18 :orchestrator.knowledge` (`reason-over`,
`knowledge-answer`), `greek-legislation-ontology.lisp:34 :orchestrator.legal-ontology` (`defconcept`, `*concepts*`),
`knowledge-graph.lisp:26 :orchestrator.graph` (`knowledge-graph`, `add-node`, `relate`). These are distinct packages
and files from `:orchestrator.memory`; they are the ontology/knowledge seats, not `MemoryType :SEMANTIC`.

**Coverage κανόνας:** EXISTING seats are reused; PARTIAL/MISSING types are typed by v1.6 and mapped to the ONE
memory seat (`memory.lisp` EXTEND) — none forks a second memory system. The 13-type memory taxonomy has NO
owning Work-Packet (`FUTURE BOOK REVISION REQUIRED`, see §7 + migration §2a); actual construction is future
work, not this pass.

## 6. PUBLIC / PRIVATE / REAL-TIME / EMBODIMENT DEPENDENCY PROOF (real TRANSITIVE closure, not a manual set)
Public build hash-bearing **roots** = `{LegalIR/1, MemoryEvent/1, TrustBundle/1, LanguageCognitionLayer/1,
CognitionResult/1}`. The proof is no longer a hand-asserted list: audit **`V6S12`** parses every record, builds
the `:type` dependency graph, computes the **transitive closure** from these roots, and asserts it contains
**no** `:DEFERRED_PRIVATE` record (`PrivateMemoryEvent/1`, `PrivateMatterProfile/1`, `RealTimeAssistance/1`,
`EmbodimentInterfaces/1`) and **no** private-bearing enum (`MemoryType` with `:PRIVATE_CLIENT_MATTER`,
`MemoryScope` with `:client`/`:matter`). The public `MemoryEvent/1` base carries only
`PublicMemoryType × PublicMemoryScope` (defect 4). Non-vacuity is proven inline: `V6S12M` injects
`:scope :type MemoryScope` (a private enum) into the public root and the closure detects the leak (flips to ≥1).
`:public-dependency nil` holds on all four extension records; the private profile **consumes** signed public
releases / proof-carrying interfaces only (`V6I-07`). Public → private/embodiment edges are FORBIDDEN and
structurally absent (`V6S10` seed = 0, `V6S12` transitive = 0). Boundary is ACYCLIC.

## 7. REGISTRY / TRACEABILITY COUNTS + WP honesty + duplicate/orphan result
- `SUBSYSTEM-REGISTRY.sexp`: **24** subsystems (18 reused CPEI + 6 v1.6) + a `define-wp-purpose` block for
  WP-00..WP-14 (audit's WP source of truth). Each subsystem ONE owner, ONE migration disposition. Duplicate
  subsystem ids = 0 (`V6S4`). Multiple write owners = 0 (`V6S5`). Every `:future-wp` resolves to a real WP
  (`V6S11` = 0; `V6S11M` flips on an injected `WP-99`).
- **WP honesty (corrected):** cognition → **WP-08** (not WP-06); TrustBundle → **WP-06** (not WP-03);
  neural/proposer/model-independence → **WP-07** (not WP-02); DeclassificationReceipt → **WP-12** (not WP-10);
  the **memory kernel taxonomy → `FUTURE_BOOK_REVISION`** (no WP owns it — not WP-11); unified adapter type →
  `WP-07+WP-11+WP-14`. Migration-table concept↔WP mapping is parse-checked (`WPX` = 0; `WPXM` flips).
- `INTERFACE-AND-SCHEMA-REGISTRY.sexp`: **28** interfaces (13 universal + `CognitionResult/1` + cognition/memory/
  proposer + 3 extension + v1.5/v1.4 families), each ONE owner + seat. Duplicate interfaces = 0 (`V6S6`).
  Referenced-but-undefined = 0 (`V6S8`). No orphan public root (`V6S16` = 0).
- No adapter-specific/vendor canonical type (`V6S9` = 0). No duplicate type definition (`V6S13` = 0). No
  public→private transitive leak (`V6S10` seed = 0, `V6S12` closure = 0). Every cognition capability mapped
  exactly once (`V6S14` = 0). No mandatory model/runtime (`V6S15` = 0). Symbolic-only path complete (`V6S17` = 0).
- TRACEABILITY-MATRIX §v1.6: detailed per-requirement rows (`R-V6-*`), each with ONE owner seat, test/falsifier,
  and future-WP impact.

## 8. ONNX / MODEL INDEPENDENCE + SYMBOLIC-ONLY PROOF (structural)
ONNX is `ONNXProposerAdapter` (`:mandatory nil :replaceable t :canonical-write-authority nil`). Every proposer
emits only typed anchored `CandidateInterpretation/1` through the non-evaluating ingress decoder; no keys, no
canonical write, no self-certification, score never legal truth, removable with no memory/data loss. With all
proposers absent ⇒ `SafetyMode :SYMBOLIC_ONLY`, a COMPLETE architectural path (`V6I-03`). OCR is
`OCRPerceptionAdapter`; same bytes ⇒ same manifestation identity.

## 9. FINITE UNKNOWN LEDGER (ρητό, πεπερασμένο)
- U-1..U-8 (v1.4 §12) παραμένουν ανοιχτά.
- Όλα τα `V6Q`/`V6KW` **UNEXECUTED** (απαιτούν κώδικα — IMPLEMENTATION BLOCKED).
- Memory types WORKING_CONTEXT/EPISODIC/PROCEDURAL/PROSPECTIVE/USER_PREFERENCE/META/SKILL: PARTIAL/MISSING
  seats — typed τώρα· owner = `FUTURE_IMPLEMENTATION_BOOK_PACKET_REQUIRED` (καμία υπάρχουσα WP δεν κατέχει memory
  kernel· substrate-επικάλυψη μόνο = WP-03 bitemporal event store). **ΟΧΙ WP-11** (v1.7 C-1 resolution).
- Πραγματικοί proposers/OCR engines/hardware devices/namespace authorities: external-operational (δεν
  εφευρίσκονται· contracts μόνο).
- Legal content `PENDING_LEGAL_VALIDATION`· measurable-corpus cognition evidence: future qualification.
- Private/real-time/embodiment: interfaces only· καμία υλοποίηση, καμία public dependency.

## 10. REGRESSIONS (honest scope)
- **Frozen v1.4** στο snapshot `88129099`: `V1.4-CONTRADICTION-OMISSION-AUDIT.sh` = 158/158 exit 0.
- **v1.5** (`V1.5-CONTRADICTION-OMISSION-AUDIT.sh`) = 75/75 exit 0 (όλα τα v1.5 repairs).
- **v1.6** (`V1.6-CONTRADICTION-OMISSION-AUDIT.sh`) = structural/document/duplicate-seat consistency + the two
  regressions above. **Honest label:** structural/document checks μόνο — **ΟΧΙ** semantic/legal/security/
  qualification proof· κανένα `V6Q`/`V6KW` δεν εκτελείται.
- Frozen `88129099` immutable (tree `a2617649`)· pinned `.out` (`4873e610`) αμετάβλητο.

## 11. STATUS
`SPEC v1.6 CURRENT CANDIDATE — NOT FROZEN — NOT QUALIFIED — IMPLEMENTATION BLOCKED`. No freeze/re-freeze, no
Implementation Book update, no WP-00, no implementation, no qualification claim, χωρίς νέα ρητή εντολή δημιουργού.
