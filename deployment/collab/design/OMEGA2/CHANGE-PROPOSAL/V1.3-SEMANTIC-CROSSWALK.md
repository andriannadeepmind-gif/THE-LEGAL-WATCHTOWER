# V1.3 SEMANTIC CROSSWALK — ΚΑΘΕ ΝΕΑ ΕΝΝΟΙΑ → ΜΙΑ ΕΔΡΑ Ή ΡΗΤΟ ΚΕΝΟ

**Σκοπός (εντολή δημιουργού):** «κάθε νέα έννοια → μία υπάρχουσα έδρα ή ρητά νέο
κενό. Καμία δεύτερη παράλληλη αρχιτεκτονική.» Αυτό το αρχείο είναι ο έλεγχος
μη-διπλασιασμού του v1.3: αν μια έννοια δεν δένει σε υπάρχουσα έδρα, δηλώνεται
**`ΝΕΟ ΚΕΝΟ`** — δεν κρύβεται.

Στήλες: **έννοια v1.3** · **έδρα** (αρχείο/σύμβολο· «—» αν κενό) · **status**
(`REUSE` = υπάρχουσα έδρα, καταναλώνεται· `EXTEND` = υπάρχουσα έδρα, χρειάζεται
επέκταση· `NEW GAP` = δεν υπάρχει, δηλώνεται· `COMPOSE` = οι έδρες υπάρχουν, η
σύνθεσή τους είναι νέα).

---

## 1. Ταυτότητα & αυθεντικότητα (v1.3 §2)

| έννοια v1.3 | έδρα | status |
|---|---|---|
| Work→Expression→Manifestation→Item | `LAWMAX-UNIVERSAL-SOURCE-CONTRACT.md §1` (`lsw1/lse1/lsm1/acq1`) | **REUSE** |
| «raw bytes ταυτοποιούν item, όχι work» (Q07 fix) | USC §1.2 `W-UNRELATED-CORPUS-IDENTITY-CHURN` | **REUSE** |
| ταυτότητα ανθεκτική σε αναταξινόμηση | USC §1.1 `W-CLASS-RECLASSIFICATION-ID` | **REUSE** |
| authority registry | USC §2.2 `authority-registry` (`auth1:`) | **REUSE** |
| institutional register | USC §2.1β `institutional-register/1` (`ireg1:`) | **REUSE** |
| authority-proof-bundle | USC §0.1 `authority-proof-bundle/1` (CLOSED & FROZEN) | **REUSE** |
| divergence witnesses (πηγές διαφωνούν) | USC §8 uncertainty `official-sources-conflict` | **REUSE** |
| «RFC-3161 = χρόνος, όχι προέλευση» | `LAWMAX-TRUST-BOOTSTRAP-SPEC.md §1` | **REUSE** (διόρθωση v1.2) |

## 2. Machine Legal Trust Protocol (v1.3 §3, MLTP)

| έννοια v1.3 | έδρα | status |
|---|---|---|
| proof-carrying certificate (αντικείμενο απόδειξης, όχι claim+sig) | `LAWMAX-PROOF-OBJECT-SPEC.md §0` (De Bruijn) | **REUSE** |
| Merkle inclusion/root | `PROOF-CARRYING-LAW.md` (RFC 9162, `lawmax-merkle-sha256-v1`) | **REUSE** |
| `SourceAuthenticityReceipt` | acquisition-receipt/1 + raw-artifact/1 + authority-proof-bundle + institutional-register | **COMPOSE** |
| `LegalStateCertificate` | legal-state-attestation/1 + knowledge-checkpoint/1 + PCL inclusion | **COMPOSE** |
| `TemporalProjectionCertificate` | version-graph snapshot-at + census-2 temporal block | **COMPOSE** |
| `CoverageAndFreshnessCertificate` | census-2 known_at + TUF timestamp — **αλλά** coverage ledger | **NEW GAP** (national census, AS-IS R-1) |
| `JurisprudenceCertificate` | M4 + USC §6.3 relations — **αλλά** ratio/obiter/line-of-authority | **NEW GAP** (Level-7, status ✗) |
| `CorrectionOrRevocationRecord` | uncertainty resolution + WITHDRAWN/SUPERSEDED + relation-retract + key revocation | **COMPOSE** |
| `TrustBundle` | census-2 + corpus-proof + trust-bootstrap tra/3 + delegation chain | **COMPOSE** |
| «7 certs → ένα εκτελέσιμο πρωτόκολλο» | — | **NEW GAP** (η σύνθεση είναι νέα) |

## 3. Offline verifier + provider integration (v1.3 §4)

| έννοια v1.3 | έδρα | status |
|---|---|---|
| minimal offline verifier (6 γραμμές, SHA-256 μόνο) | `PROOF-CARRYING-LAW.md §5-6` | **REUSE** |
| LOC-ceiling gate / kernel diversity | `LAWMAX-PROOF-OBJECT-SPEC.md §4` | **REUSE** |
| pinned trust roots (out-of-band, ≥2 κανάλια) | trust-bootstrap §3 + key-lifecycle §2.2 | **REUSE** |
| key rotation/revocation (append-only, kid/alg/lineage) | `LAWMAX-KEY-LIFECYCLE-SPEC.md §2-3` | **REUSE** |
| transparency log + consistency proofs | trust-bootstrap §4 (RFC 9162 §2.1.2) | **REUSE** |
| external witnesses / gossip (split-view) | trust-bootstrap §4 | **REUSE** |
| versioned MCP | `source/mcp-server.lisp` (4 εργαλεία) | **EXTEND** |
| εκδοχοποιημένο OpenAPI | — | **NEW GAP** (AS-IS EV-5: κανένα) |
| SDKs offline verification | — | **NEW GAP** |
| provider rule → `UNVERIFIED_FOR_MACHINE_RELIANCE`/`UNKNOWN` | v1.2 North Star (ρητό UNKNOWN) + freshness | **COMPOSE** |

## 4. Νομολογία — Level-7 plane (v1.3 §5)

| έννοια v1.3 | έδρα | status |
|---|---|---|
| «Νομολογιακή συνείδηση-εξέλιξη» | `LAWMAX-CEILING-CROSSWALK.md` Level 7 (status **✗**, phase Ω7β) | **NEW GAP** (κατατεθειμένη σχεδίαση) |
| ratio/obiter, holding, legal issue, disposition | Level-7 + M4 τρίτιμο | **NEW GAP** |
| typed relations (follows/distinguishes/annuls/…) | USC §6.3 kinds | **REUSE** |
| authority weight (μετρημένο, όχι γνώμη) | CEILING-CROSSWALK #15 φρουρός («ΜΟΝΟ γειωμένο σε πηγές») | **REUSE** (αρχή) / **NEW GAP** (μηχανισμός) |
| temporal line-of-authority graph | L2 bitemporal graph (`:law`) | **EXTEND** |
| separate opinions | — | **NEW GAP** |

## 5. Cockpit signed intent (v1.3 §6)

| έννοια v1.3 | έδρα | status |
|---|---|---|
| cockpit ως έδρα | `systems/orchestrator-cli/cockpit.lisp` (4 capabilities) — AS-IS R-2 (REPORTED) | **EXTEND** |
| InstitutionalAct / envelope (μία έδρα) | `LAWMAX-CPEI-TARGET-SPEC.md §2` (`%ask-envelope`) | **REUSE** |
| `signed_proposal_intent` / `signed_approval_intent` | envelope πεδίο + κλειδί ενεργούντος | **EXTEND** |
| «ποτέ παράκαμψη M5» | M5 proposer-blind (v1.2) — δομικά μη publish από cockpit | **REUSE** |
| RBAC/MFA στον ενεργούντα | — | **NEW GAP** (AS-IS: καμία αυθεντικοποίηση) |

## 6. Root Authority ως ανακλητή κατάσταση (v1.3 §7)

| έννοια v1.3 | έδρα | status |
|---|---|---|
| freshness-bound | TUF `timestamp` role (key-lifecycle §1) + census-2 known_at | **REUSE** |
| externally verifiable metrics | trust-bootstrap §4 witnesses + v1.2 mission measures | **REUSE** |
| revocable (υπογεγραμμένο / αυτόματο) | key-lifecycle §2.5 revocation | **REUSE** |
| continuous/time-bounded state | — | **NEW GAP** (η κατάσταση-ως-κατάσταση είναι νέα) |
| provider-adoption qualification (ξεχωριστό) | — | **NEW GAP** (νέα βαθμίδα, Q26/Q27) |

## 7. Ταξινόμηση στόχων (v1.3 §0, SUPERSEDED-REGISTER)

| έννοια v1.3 | έδρα | status |
|---|---|---|
| PUBLIC target ≠ PRIVATE target, one-way | v1.2 §3.2 + CPEI `:matter` L5-L7 | **REUSE** |
| CPEI = DEFERRED/SEPARATE PRIVATE (NOT SUPERSEDED) | `LAWMAX-CPEI-TARGET-SPEC.{md,sexp}` + CEILING-CROSSWALK | **REUSE** (νέα ταξινόμηση) |
| ARCHITECTURE-CONSTITUTION = ACTIVE ENFORCED | `LAWMAX-ARCHITECTURE-CONSTITUTION.sexp` + `--architecture-constitution-gate` 12/12 | **REUSE** |
| PCL/trust/key/temporal/USC = ACTIVE SHARED TRUST FOUNDATIONS | οι 8 specs | **REUSE** |

---

## ΣΥΝΟΨΗ ΚΕΝΩΝ (τα `NEW GAP` που το v1.3 ΔΕΝ κρύβει)

1. **Coverage ledger / εθνική απογραφή** — το κεντρικό μηχανικό κενό του δημόσιου
   στόχου (AS-IS R-1). Κάθε `CoverageAndFreshnessCertificate` εξαρτάται από αυτό.
2. **Level-7 jurisprudence plane** (ratio/obiter/holding/line-of-authority) —
   κατατεθειμένη σχεδίαση, status ✗· βάση του `JurisprudenceCertificate`.
3. **Εκδοχοποιημένο OpenAPI + SDKs** — καμία API έδρα σήμερα (AS-IS EV-5).
4. **RBAC/MFA** στον ενεργούντα (cockpit intent + review) — καμία αυθεντικοποίηση.
5. **Η σύνθεση των 7 πιστοποιητικών** σε ένα εκτελέσιμο πρωτόκολλο — οι έδρες
   υπάρχουν, η ένωση είναι νέα.
6. **Root Authority ως συνεχής/ανακλητή κατάσταση** + **provider-adoption
   qualification** — νέες έννοιες κατάστασης/βαθμίδας.

**Καμία από αυτές δεν ανοίγει δεύτερη αρχιτεκτονική.** Κάθε `NEW GAP` είναι
capability/σύνθεση πάνω στο ίδιο υπόστρωμα (CPEI §4 αρχή: memory types ≠ stores·
εδώ: certificates ≠ νέα subsystems).
