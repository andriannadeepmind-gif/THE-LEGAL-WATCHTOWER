# LAWMAX-Ω CANON — START HERE (session bootstrap)

This package is the **single canonical design** of the supreme legal system and the
**complete execution program** for transforming the current repository
(`andriannadeepmind-gif/THE-LEGAL-WATCHTOWER`, baseline commit `e621dbe1`, "B0")
into it. It supersedes every prior study package (all prior material is preserved
verbatim under `ARCHIVE/`). It is written to be handed to a fresh session with zero
prior context.

## Read order (mandatory, top-down)

1. `00-MISSION.md` — what the system is for; the exact definition of "unbeatable at the level of the feasible".
2. `01-INVARIANT.md` — the Root Invariant, the two axioms, the logical supremacy proof, and the Method (self-repairing supremacy).
3. `02-ARCHITECTURE.md` — three layers; five pillars **derived** from the invariant, with all sharpenings native.
4. `03-ORGANS.md` — the virtual-firm faculty layer (multi-model, all untrusted), trust grade per organ.
5. `04-OFFICE.md` — the whole office pipeline under the same trust discipline; the public/private wall.
6. `05-GROUND-B0.md` — what the repository actually runs today; defect classes; evidence pointers.
7. `06-TRANSITION.md` — the build program: BO-01..BO-33, phased P1..P7, with B0 file anchors and exit criteria.
8. `07-VERIFICATION.md` — what VERIFIED means; adversarial protocol; backtesting; builder protocol; permitted claims per status.

`GR/` contains plain-Greek mirrors of 00–07 for the creator.
`EVIDENCE/` contains the untouched proof-of-search records (census, adversary verdicts, tournament, frozen instruments).
`ARCHIVE/` contains every superseded artifact verbatim, including the entire previous
deliverables package (`superseded-SSP-deliverables/`) — nothing has been deleted.
`TOOLING/lean-proofs.yml` is the fail-closed CI workflow for machine-checking the proof corpus (Phase 1).

## Binding constraints (from the creator contract — CLAUDE.md in the repo; they override everything)

- Highest implementation only; **no patches, no wrappers, one seat per concept**; honest ignorance; no LLM in the trusted path.
- **Only the creator approves/merges phases** — explicit «εγκρίνω <phase>» per phase; nothing self-opens.
- Develop only on the designated feature branch; **no repo write** without a creator-approved delta.
- Commit identity ALWAYS `Stavropoulos Law® <info@stavropouloslaw.com>`; **no AI trailers, no model names or AI references in any repo artifact**; restore `deployment/self/history.sexp` and `output/.healthy` (`git checkout --`) before every commit.
- License: All Rights Reserved everywhere (Deferred License Policy Decision pending for inherited CC0 texts — only with explicit creator approval).
- Every phase: plan → explicit creator approval → implementation → **internal adversarial review** (fresh-context agents; break-the-design axis + mediocrity-hunt axis) → finding closure → full proof (gates/tests/audits with numbers) → owner docker proof → explicit merge order.

## Canon change law

Every future change to this package must be **derivable from `01-INVARIANT.md`** and must
**rewrite its seat in place**; a change that arrives as an addendum, appendix, "v2" file, or
side-file is rejected on sight. That rule is what keeps this canon permanently
"designed from scratch".

## How to begin execution

Start at `06-TRANSITION.md`, Phase P1 (proof-toolchain unblock via `TOOLING/lean-proofs.yml`
on GitHub Actions). Do not begin any phase without the creator's explicit «εγκρίνω P<n>».
