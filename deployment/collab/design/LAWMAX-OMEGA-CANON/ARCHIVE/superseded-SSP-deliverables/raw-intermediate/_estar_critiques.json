[
 "Adversarial review complete. Lens: mediocrity-floor. The dossier is heavily pre-armored (nearly every cell pre-emptively says \"NOT a bigger number of the same\"), but the armor fails in specific, quotable places. Findings ranked by severity.\n\n---\n\n## P0 — AX-22 (cross-legal-order generality): the only \"provable now\" evidence IS the named floor\n\n**Quoted:** *\"already emits byte‑canonical proof‑carrying artifacts across the SIX Greek codes (astikos, poinikos, kpolitikis, kpoinikis, kdioikitikis, constitution … all present under `output/`)\"* and proof_status `ARCH_CLASS_PROVABLE_NOW`, against a delta_definition that itself demands *\"ratio 1.0 … demonstrated across >= 2 legal orders.\"*\n\n**Why it is floor:** All six codes are one legal order — the Greek national system, one language, one ELI/FRBR profile, one constitutional frame. Reuse across civil/penal/procedure/constitution is reuse across **subject matter**, i.e. corpus breadth — which the cell itself names as the floor it claims to beat (*\"floor's generality is corpus scale … more documents re‑ingested\"*). By its own pre-registered threshold (≥2 legal orders), the now-claim is unmet; the genuinely cross-order test is deferred to `NEEDS_EXECUTABLE_BENCHMARK`. So the axis about crossing legal orders is substantiated entirely by breadth within one order = the floor, restated.\n\n**Correction (strictly-higher mechanism):** Replace the six-Greek-codes evidence with an actual guarantee-transport proof across ≥2 **structurally distinct** orders (e.g. a common-law jurisdiction where the *source* is case law, or at minimum a non-Greek civil-law order in a different language/ELI profile), re-measuring core-invariance via `git log -S`. Until that exists, `proof_status` for AX-22 must drop from `ARCH_CLASS_PROVABLE_NOW` to blocked/benchmark. The honest-ignorance law requires this downgrade.\n\n---\n\n## P1 — AX-17 (latency): determinism (AX-14) restated, with op-count identity falsely equated to a wall-clock bound\n\n**Quoted:** *\"the same frozen workload replays to a bit‑identical trace and identical operation/step count, so the p100 tail on the trusted path is a deterministic function of the input, not a random variable.\"*\n\n**Why it is a restatement / category error:** \"Same op count ⇒ deterministic p100 tail\" is false — identical op counts do not bound wall-clock latency (GC, cache, scheduler, load vary it freely). What is actually delivered is *op-count determinism*, which is nothing but AX-14's no-LLM determinism projected onto a latency label. The real latency quantity (wall-clock ms) is punted as `NEEDS_EXECUTABLE_BENCHMARK` — a bigger-number-of-the-same.\n\n**Correction:** The strictly-higher mechanism is a proven worst-case step bound over the pure kernel — `trusted_decision_steps ≤ f(|input|)` as a theorem — plus a declared step→time resource model under stated hardware assumptions (a WCET-style envelope). Absent that, reclassify AX-17 honestly as a corollary of AX-14, not an independent dominance axis, and delete the \"deterministic p100 tail\" claim.\n\n---\n\n## P1 — AX-18 (throughput/scale): the axis is answered by re-labeling AX-13/AX-04, and the real throughput number is deferred\n\n**Quoted:** *\"The throughput UNIT is a proof‑carrying artifact … each unit of output is a provision‑with‑inclusion‑proof to a signed Merkle root … plus its execution‑trace, INDEPENDENTLY re‑verifiable offline in O(log n).\"*\n\n**Why it is a restatement:** The \"throughput unit\" is verbatim AX-13's proof-carrying artifact / AX-04's Merkle inclusion, relabeled. The only content actually about scale is the RFC 9162 consistency proof (old⊑new in O(log n)) — standard transparency-log tech (Certificate Transparency), an **audit-cost** property, not a throughput property. The genuine throughput metric (*\"verified‑artifacts‑per‑core‑second\"*) is a raw bigger-number deferred to benchmark. The axis \"throughput, scale & resource-normalized performance\" is never engaged; a verification-cost-class is substituted for it.\n\n**Correction:** Either (a) rename the axis to \"incremental-audit-cost\" and fold the proof-carrying half back into AX-13 so it is not double-counted, or (b) supply a real throughput/scale mechanism — a proven amortized admitted-cost bound per artifact and a liveness-under-load / back-pressure guarantee. Do not let O(log n) *audit* cost masquerade as throughput dominance.\n\n---\n\n## P2 — AX-19 (introspection/explanation): identical substrate to AX-13, re-projected as a separate axis\n\n**Quoted:** *\"a typed, data‑only … span DAG (`execution-trace`) … a Merkle inclusion proof to the signed corpus root for every cited authority … re‑verifiable offline by verify-provision-proof.\"*\n\n**Why it is bigger-number-of-axes:** This is the exact span-DAG + inclusion-proof mechanism of AX-13, plus AX-09's honest-ignorance gate. Nothing structurally new; \"explanation\" is \"auditability\" relabeled.\n\n**Correction:** Merge into AX-13, or give AX-19 a property AX-13 lacks — e.g. a minimal-sufficient-cause / counterfactual proof object (which trace steps were load-bearing), not the same re-verifiable chain renamed \"causal account.\"\n\n---\n\n## P2 — AX-03 (coordination/parallelism): \"DAG-verified scheduling\" is workflow-engine floor\n\n**Quoted:** *\"Parallelism is SCHEDULED OFF THE VERIFIED DAG … `parallel-executor.lisp` runs stages via lparallel, launching only stages whose dependencies are all met\"* and, in the B-COMM-03 cell, *\"the dominator is NOT team size but DAG‑verified scheduling + provably single canonical writer.\"*\n\n**Why it is floor:** DAG scheduling with dependency-gated launch is exactly what Airflow/Temporal/Dagster/make do — commercial floor, not a tier above it. Naming \"DAG-verified scheduling\" as a *dominator* is the mediocrity.\n\n**Correction:** Strike DAG scheduling and lparallel parallel execution from the dominance basis. The only strictly-higher element on this axis is single-writer serialized arbitration with deterministic replay (unique-latest + monotonic-sequence + capability-closure). State AX-03's delta on that alone.\n\n---\n\n## P2 — AX-07 (matter memory): \"13 kinds / 8 stores\" is bigger-number dressing\n\n**Quoted:** *\"a typed cognitive taxonomy (13 kinds) over 8 canonical stores.\"*\n\n**Why:** Kind-count and store-count are quantity, not tier; \"matter memory\" is a named floor item and a larger taxonomy over it is more-of-the-same. The genuine tier-lift is only the SHA-256 hash chain + single-writer-per-store + the *computed* (not stored) `memory_recorded` invariant.\n\n**Correction:** Drop the taxonomy counts from the dominance argument; rest AX-07 solely on tamper-evidence (undetectable tampering made impossible) + single-writer + computed-recorded-flag + human-signed-only consolidation.\n\n---\n\n## P2 — AX-02 (plan completeness): topological sort is build-system hygiene, not a strict tier over \"reusable workflows\"\n\n**Quoted:** *\"`build-dependency-graph`, `topological-sort`, and cycle detection … validate-pipeline is promoted to a total decision that admits a plan only if … the graph is acyclic (topological-sort succeeds).\"*\n\n**Why:** Topo-sort + cycle detection + type-closure is standard compiler/build-system correctness, present in every serious workflow engine. Presenting a topo-sort validator as strict dominance over CoCounsel/Protégé/Harvey overstates an engineering-hygiene feature.\n\n**Correction:** Concede it as correctness (not a dominance delta), or raise to a genuinely higher mechanism — a total planner carrying a proven completeness theorem (goal-artifact reachability decided, not merely graph acyclicity checked).\n\n---\n\n## P2 (meta) — one mechanism, 22 projections, read as 22 independent dominance results\n\n**Quoted (the dossier's own admission):** *\"Everything in Section 3 is a projection of these three boundaries onto one axis of comparison.\"*\n\n**Why it is bigger-number-of-axes:** Axes 13/17/18/19 share the identical span-DAG + Merkle + determinism substrate; axes 03/11/15 share the single-writer journal seat. A 22-cell \"66-cell dominance matrix\" whose breadth is one certificate-emitting kernel re-labeled 22 ways inflates apparent coverage.\n\n**Correction:** Mark co-substrate cells as explicit projections in the matrix (or collapse them), so \"22 axes\" is not read as 22 independent structural results. This is a presentation-integrity fix, not a mechanism defect.\n\n---\n\n**Axes that genuinely clear the floor (no finding):** AX-01 (defeater-closure + LLM-excluded), AX-04 (canonical-identity + completeness invariant vs top-k RAG), AX-05 (hermetic re-derivation vs asserted flag), AX-06 (byte-exact replay + gating completeness), AX-09 (measured double-bounded honest ignorance), AX-10 (generate-and-defeat-own-counter-design as merge gate — the real LAWMAX signature), AX-11/12/13/14/15/16/20/21. These are structural non-existence guarantees, not floor restatements.\n\n**Not NONE.** The floor-restatement and bigger-number failures are concentrated in the \"performance-shaped\" axes (17 throughput-latency, 18, and the generality axis 22) — precisely where a projection of the certificate mechanism cannot manufacture a genuinely new tier, so the dossier fell back to determinism-restated (17), audit-cost-for-throughput (18), and corpus-breadth-for-cross-order (22).",
 "Repo-grounded. Confirmed against source: `admission-model.sexp` is `:implementation-status :specification-only` / `:assurance-status :under-construction`, **all 9 theorems T1–T9 (incl. T6) are `:blocked-toolchain`**; `authority-v2/log/witness-policy.sexp` carries `:external-quorum-status :disabled` and `:split-view-resistance-claim nil`; the AX‑09 honesty counters exist in-repo only as test-assertion strings/comments, not standing computed invariants; `deployment/verify/capability-baseline.sexp` records raw counts (E2E‑OK 8/13), not per‑metric floors. Findings, ranked:\n\n---\n\n**P0‑1 — The kernel K is spec‑only with every theorem blocked, yet cells assert its guarantees \"by construction\" as established fact.**\n`admission-model.sexp` header: *\"ΔΕΝ υλοποιείται σε Common Lisp … :implementation-status :specification-only\"* and all nine theorems `:status :blocked-toolchain`. Yet §2.3 states, unqualified: *\"split‑brain / double‑writer / gap‑insertion are admissible‑state‑count = 0 **by construction**.\"* AX‑03: *\"double‑writer admission rate 0\"*; AX‑11: *\"11 machine‑checked invariants/commit vs 0\"* and *\"each orchestrated step either commits atomically … or changes nothing\"* (present tense). These treat an unimplemented, unproven specification as a delivered structural guarantee — precisely a NEEDS‑discharge obligation dressed as established superiority.\n**Correction:** every K‑derived guarantee must read \"0 *conditional on* discharge of T1–T9 (currently `:blocked-toolchain`) *and* implementation of the spec.\" Replace \"by construction\" / \"machine‑checked\" with \"spec‑asserted, theorems undischarged.\" \"11 machine‑checked invariants\" → \"11 *specified* conjuncts, 0 machine‑checked.\"\n\n**P0‑2 — AX‑14 and AX‑17 assert quantitative \"= 0\" results as `ARCH_CLASS_PROVABLE_NOW` while their own residual says the benchmark has not been run and T6 is blocked.**\nAX‑14 `evidence_mode: quantitative`, `proof_status: ARCH_CLASS_PROVABLE_NOW`, claims *\"reproduction error = 0 bits\"* — but its residual: *\"NEEDS_EXECUTABLE_BENCHMARK … run the E_star pipeline N times and certify 0‑bit diff.\"* AX‑17: *\"reproducibility error = 0 operations\"* / *\"bit‑identical trace\"* as PROVABLE_NOW, residual concedes the wall‑clock/run is NEEDS_EXECUTABLE_BENCHMARK. And T6‑deterministic‑replay is `:blocked-toolchain`. A cardinal \"= 0\" that still requires the run to be executed is not provable now.\n**Correction:** the quantitative sub‑claim's `proof_status` must be `NEEDS_EXECUTABLE_BENCHMARK`, not `ARCH_CLASS_PROVABLE_NOW`. Only the *existence of a deterministic partition* (logical) is provable‑now; the \"0 bits / 0 ops\" number is unmeasured.\n\n---\n\n**P1‑3 — Pervasive \"not declared ⇒ provably/structurally lacks\": UNKNOWN about a competitor asserted as established absence, violating honest ignorance.**\nAX‑07: *\"the integrity properties follow from the baseline's **DECLARED absence**\"* and *\"A NEEDS_BLACK_BOX_PRODUCT_RUN would only arise if one wished to disprove an unpublished hidden integrity feature\"* — an explicit admission the baseline *might* have the feature, i.e. the comparison is UNKNOWN, not strict. AX‑13 states *\"recomputable‑chain fraction 1.0 vs **0**\"* yet its residual concedes the baseline count is *\"exactly 0 rather than **≤1**\"* — undetermined. Honest ignorance forbids collapsing \"not disclosed by vendor\" into \"structurally 0.\"\n**Correction:** replace every \"provably/structurally lacks\" with \"does not *declare* (competitor internals unknown)\"; downgrade all \"vs 0\" baseline cardinals to \"vs unknown\"; the dominance may be claimed only over the baseline's *declared* set, and must say so in each strictness clause, not silently upgrade to \"structural.\"\n\n**P1‑4 — Hidden weakness: the split‑brain / coordination guarantee is contradicted by a disabled seat, surfaced in one buried residual only.**\n§2.3 and AX‑03 headline \"split‑brain … = 0 by construction.\" But `witness-policy.sexp`: `:external-quorum-status :disabled`, `:split-view-resistance-claim nil`. Only AX‑03's B‑COMM‑02 residual admits *\"External witness‑quorum split‑view resistance is explicitly :disabled.\"* The headline non‑existence claim hides a whole failure mode (distributed split‑view) that the system explicitly does not defend.\n**Correction:** scope the guarantee to \"single‑host single‑writer; distributed split‑view resistance is `:disabled` / not claimed\" everywhere the \"0 split‑brain\" claim appears — not only in one sub‑cell's residual.\n\n**P1‑5 — AX‑09 honesty counters are test‑tautologies presented as measured structural invariants.**\nAX‑09 delta: *\"the honesty counters are exact integers with resolution 1 … strict threshold is EXACTLY 0.\"* In‑repo, `false_uncertainty=0` appears only inside a **test description string** (`tests/temporal-semantics-test.lisp`) and `unverified_satisfactions=0` only in a **comment** (`source/version-graph.lisp`); `silent_scope_omissions` is not a computed value anywhere searched. So \"= 0\" is the test's own expected value on its own fixtures — a test‑tautology the creator law names explicitly (\"τεστ‑ταυτολογίες\"). It cannot ground strict dominance.\n**Correction:** relabel these as \"0 on the current fixture suite (snapshot metric)\"; they are not machine‑measured standing invariants over inputs and must be removed from the strict‑dominance instrument until computed and adversarially witnessed.\n\n**P1‑6 — AX‑21 inflates operational assurance rank to \"2–3 / machine‑checked proof‑object\" while T9 certificate‑soundness is blocked.**\nAX‑21: *\"E_star operational rank 2‑3 today\"* with rank 3 defined as *\"machine‑checked proof‑object with an independent checker (bidirectional certificate soundness).\"* But T9‑certificate‑soundness is `:blocked-toolchain`, and the \"independent checkers\" (`verify-proof-manifest.py`) validate manifest/format, not theorem soundness. No proof object is machine‑checked.\n**Correction:** operational rank is **2** (deterministic non‑LLM validator + declared TCB + format checkers); \"2–3\" and \"machine‑checked proof‑object / bidirectional soundness\" are not earned until T9 discharges. Drop the \"3\" and the range hedge.\n\n---\n\n**P2‑7 — Gap magnitudes (\"Gap = 2\", \"Rank gap = 3 steps\", \"R3 vs R0 = 3 full steps\") read as compensation on partial‑order lattices.**\nThe non‑compensation rule supports only \"≥1 strict coordinate, none weaker.\" Reporting a scalar *magnitude* invites the reading that a big lead on one coordinate offsets others, and treats a partial order as a totalized integer scale.\n**Correction:** state per‑coordinate strict/equal/weaker and the single bit \"≥1 strict, none weaker\"; delete the \"= 2 / = 3 steps\" magnitudes or mark them non‑load‑bearing decoration.\n\n**P2‑8 — AX‑22 claims \"generality across legal orders\" from six Greek codes (one legal order); genuine cross‑order is unrun, and the \"ratio 1.0\" measurement is asserted, not shown.**\nThe six codes (astikos/poinikos/…/constitution) are bodies of law within a *single* Greek civil‑law order sharing one ELI/FRBR spine; the residual concedes a *foreign* order is `NEEDS_EXECUTABLE_BENCHMARK`. \"Core‑invariance ratio 1.0 … measured via git log -S\" is presented as done but no measurement output is cited.\n**Correction:** scope the demonstrated claim to \"6 codes within the Greek legal order\"; mark cross‑legal‑order portability `NEEDS_EXECUTABLE_BENCHMARK`; mark ratio 1.0 \"pending the stated git log -S / hearth‑count measurement,\" not established.\n\n**P2‑9 — Capability‑superset ledger equivocates on the trust boundary and again reads \"b lacks X\" from non‑declaration.**\n§4 claims `Capabilities(E_star) ⊋ Capabilities(b)` by retaining *\"agentic … drafting/review, RAG\"* as E_star capabilities — but those live in the **untrusted LLM producers outside the Θ13 boundary**, contradicting the \"no LLM in trusted path\" thesis when counted as E_star's own. And *\"each element is provably outside every baseline's declared capability set\"* again converts non‑declaration into proven exclusion.\n**Correction:** state the superset over *declared* floor capability only, note the retained floor caps are untrusted‑zone (not trusted‑core) capabilities, and replace \"provably outside\" with \"outside the *declared* set (internals unknown).\"\n\n**P2‑10 — AX‑20 fail‑closed floor gate cites a baseline file that holds raw counts, not per‑metric floors.**\nAX‑20: *\"refuses any release below floor on ANY metric.\"* `capability-baseline.sexp` records `E2E-OK 8` of `E2E-TOTAL 13` etc. — a partial‑pass snapshot of raw tallies, with no per‑metric floor thresholds or comparator tokens found. The \"monotone ratchet against a committed floor vector\" is asserted ahead of the floor vector actually being populated.\n**Correction:** mark the capability‑floor gate \"wiring pending — committed baseline currently records raw counts (E2E 8/13), not per‑metric floors\"; strict no‑regression cannot be claimed until floors and the negative‑witness Reject/Accept pair are populated and run.\n\n---\n\nNot NONE. The dossier's §5 honesty ledger (66/66 `ARCH_CLASS_PROVABLE_NOW`, 0 `VERIFIED`, T6 undischarged, `FINAL_OPTIMALITY_BLOCKED`) is genuinely fail‑closed and is the document's strongest feature — but it does not cure the per‑cell defects above, where present‑tense cardinal claims (\"rate 0\", \"= 0 bits\", \"fraction 1.0 vs 0\", counters \"at exactly 0\") and \"structurally lacks\" language repeatedly promote blocked‑theorem, unrun‑benchmark, and competitor‑unknown states into asserted strict superiority inside cells all stamped `ARCH_CLASS_PROVABLE_NOW`.",
 "Investigation complete. I have what I need to render a verdict grounded in the actual repo.\n\n---\n\n# ADVERSARY REPORT — lens: superset-and-buildability\n\nVerdict: **NOT NONE.** Four findings. One is P0 and it collapses the spine of the dossier. Findings are grounded in the repo, not in the dossier's self-description.\n\n---\n\n## P0-1 — VAPORWARE: the kernel `K` (the entire E_star thesis) does not execute and is deliberately not implementable in this repo\n\n`K` is the single load-bearing object of the whole dossier. The Section 1 thesis: *\"admission is decided by `K(old_state, candidate, evidence, policy) → Reject(all‑reasons) | Accept(new_state, certificate)`\"*, drawn in the architecture diagram inside the \"SINGLE‑WRITER INTEGRITY CORE\" as an operational seat, and asserted as executable across AX‑03, AX‑11, AX‑12, AX‑16, AX‑20 (e.g. AX‑11: *\"each orchestrated step either commits atomically as a signed `transition_certificate` or changes nothing\"*; AX‑20: *\"Every evolution step of E_star is admitted only through a pure, TOTAL state‑transition kernel K\"*; AX‑12: *\"the `transition_certificate` produced by K\"*).\n\nThe actual file `authority-v2/kernel/admission-model.sexp` says the opposite, in its own header and status block:\n\n> `:assurance-status :under-construction`\n> `:implementation-status :specification-only`\n> `:implementation-language-target \"F* (verified) — ΟΧΙ Common Lisp\"`\n> *\"ΔΕΝ υλοποιείται σε Common Lisp: μια CL υλοποίηση θα ήταν δεύτερη έδρα...\"* (\"It is NOT implemented in Common Lisp; a CL implementation would be a second seat...\")\n\nCorroboration in the repo (not inference):\n- `grep` for the conjunct names (`census-completeness`, `sequence-monotonic`, `profile-continuity`, …) across all `.lisp`: **zero hits**. No executable checks the 9 conjuncts.\n- `grep` for any `transition-certificate` / cert emitter across all `.lisp`: **zero hits**. `authority-v2/schema/transition-certificate.cddl` is a CDDL *schema* for a certificate that **no code emits**.\n- The only `Accept(`/`admit-candidate`/`run-admission` match in the entire tree is inside `third-party/cl-yacc`. There is no `K` proxy under any other name.\n- `K`'s stated target (F*/Coq) is, by the dossier's own §5.3(3), `:blocked-toolchain` (\"F*/Coq/CompCert absent in‑image / network 403\").\n\nSo today: no CL implementation (forbidden by design), no F* implementation (toolchain blocked), no certificate emitter. `K` runs **nowhere**. Every cell that treats `K` as an operating commit boundary, arbitration function, or certificate source is describing vaporware. The honest, self-consistent reading is that AX‑03/11/12/16/20's *mechanism* is a `.sexp` spec plus real-but-separate seats (journal atomicity, capability-closure topology) that are **not** the 9‑conjunct kernel.\n\n**Honest blocker / fix:** Either (a) restate every \"commits as a signed `transition_certificate` via K\" claim as `SPEC_ONLY / :blocked-toolchain` — i.e. the guarantee-class is *designed*, and what runs today is the journal ratchet + OS capability closure, which enforce a **strict subset** of the 9 conjuncts (single-writer, monotonic sequence, no-fork) and emit **no certificate**; or (b) accept a CL reference implementation of `K` — which CLAUDE.md's \"μία έδρα ανά έννοια\" and the file's own comment explicitly forbid. As written the dossier violates its own \"τίμια άγνοια / κανένα μπάλωμα\": it presents a specification-only, intentionally-unbuilt kernel as the running core.\n\n---\n\n## P1-2 — SUPERSET GAP (all three baselines): the generative/agentic-research capability is neither present nor contained\n\nSection 4 asserts `Capabilities(E_star) ⊋ Capabilities(b)` and that E_star *\"still performs all declared floor capabilities of b (agentic multi‑step research/analysis/**drafting/review**, RAG grounding, … )… retained as subordinate checks.\"* Dominance is defined to **require** this containment.\n\nRepo reality: this is `README.md`'s **\"Greek Legal Corpus Orchestrator … Semantic infrastructure for Greek legal data\"** — an authority/provenance/consolidation pipeline. The `⊇` half fails on the generative capabilities:\n- The untrusted **LLM candidate‑producer** box in the §2.1 diagram (*\"LLM/agent candidate producers\"*) is **not implemented**. `candidates/` contains a single 830‑byte `README.md` describing it as the producer's **write directory** (`ORCHESTRATOR_OUTPUT_DIR=/app/candidates`, to avoid EROFS) — not a generator. No LLM/agent integration exists anywhere in the trusted or untrusted path.\n- E_star's only \"drafting\" is `systems/orchestrator-cli/draft-commands.lisp`: a **deterministic, grammar-based subsumption memo** (\"ΣΥΝΘΕΣΗ υπαρχόντων ειδικών — ΚΑΜΙΑ νέα λογική … ίδια αφήγηση ⇒ byte‑ίδιο σώμα\"), explicitly no-LLM, confined to a parsed narrative over the six present Greek codes. That is **not** open-domain agentic legal research/review across a jurisdiction's law.\n\nSo for B‑COMM‑01, ‑02, and ‑03 separately, the declared capability \"agentic multi‑step research / fluent drafting / review-ready outputs over open questions\" is **not provably contained** in E_star — the containment rests on an unbuilt untrusted-generator layer. The 22-axis \"dominance\" is therefore unproven at its root, because dominance is defined as strict superset and the superset direction is unestablished.\n\n**Fix / honest blocker:** State plainly that E_star currently contains the baselines' *grounding, citation-validation, provenance, memory, workflow-DAG, and consolidation* capabilities but **not** their generative/agentic-research capability, and that closing the superset requires building the untrusted candidate-generation layer (`candidates/` producers) — until then Section 4's `⊋` is a design target, not a proved containment. The current \"retained as subordinate checks\" wording asserts a present capability the repo does not have.\n\n---\n\n## P1-3 — VAPORWARE: the AX‑01 defeater‑closure admission gate does not exist\n\nAX‑01 (the flagship correctness axis) mechanism: *\"a conclusion node cannot enter `:accepted` unless (a) its rebutting/undercutting/statutory‑exception defeater set is ENUMERATED, (b) each defeater carries an explicit status token {defeated | undefeated | UNKNOWN} …\"*, and the cell claims *\"the executable Lisp gate already delivers the guarantee‑class.\"*\n\n`systems/orchestrator-epistemic/negation-layer.lisp` contains **one** `defun`, whose body emits an `owl:AllDisjointClasses` Turtle string (\"Generate canonical negation layer using OWL disjointness\"). There is **no** defeater enumeration, **no** `{defeated|undefeated|UNKNOWN}` status tagging, and **no** `:accepted` admission gate. The dossier's own language betrays this — AX‑01 says \"**extend** … **into** a defeater‑closure gate\" and §6 MODIFY lists \"**Extend** `negation-layer.lisp` **into** the defeater‑closure admission gate\" — i.e. unbuilt. The claim \"the executable Lisp gate already delivers the guarantee-class\" is false: the gate does not exist.\n\n**Fix:** Downgrade AX‑01's `proof_status` from a delivered executable guarantee to `DESIGN_ONLY` (buildable, no hard blocker), and stop asserting an existing executable gate. This is \"designed, not built,\" not \"not buildable\" — but the cell misrepresents it as running.\n\n---\n\n## P2-4 — Certificate emitters asserted as mechanisms are acknowledged-unbuilt; and one \"executable proof\" is env-blocked, not passing\n\nTwo smaller consistency defects:\n\n1. §6 ADD concedes the per-axis certificate emitters are unbuilt: *\"Per‑axis certificate emitters where a seat produces a conclusion but not yet a portable certificate (AX‑01 defeater‑rank certificate; AX‑17 signed latency‑envelope trace; AX‑20 monotone‑capability certificate).\"* Yet AX‑17's cell states the mechanism *\"yields a REPLAY‑CERTIFIED LATENCY ENVELOPE … A signed latency‑trace … lets any third party REPLAY the run … offline\"* as if extant. `source/execution-trace.lisp` (the data-only span DAG) exists, but the **signed latency envelope / p100 certificate** it describes has no emitter. Fix: mark AX‑01-cert / AX‑17 / AX‑20-cert mechanisms `DESIGN_ONLY`, consistent with §6.\n\n2. AX‑03 leans on `verify-capability-closure.sh` as a running instrument (*\"EXECUTES real write attempts … exit code (0 = every non‑writer identity denied …)\"*). The script is genuine, but it **returns BLOCKED (exit 2), never PASS**, unless run as `uid 0` with `setpriv`: `if [ \"$(id -u)\" -ne 0 ] || ! command -v setpriv … \"BLOCKED — NOT EXECUTED\"`. In the plain SBCL image this proof does not pass; it only passes under the privileged Docker E2E. Fix: state that the capability-closure proof is **conditional on a root+setpriv/Docker environment**, not a proof that \"already runs\" in-image.\n\n---\n\n## What is genuinely built (so the P0 is not mistaken for total vaporware)\n\nTo keep the adversary honest — these seats are real, substantial, and not vaporware; the dominance *design* rests on them, and only the *kernel-level* claims are hollow:\n- `source/journal.lisp` — real atomic `rename(2)` + `fsync`, RATCHET‑1 honest durability (`SYNC-FAILURE`, `*fsync-fault*` injection), RATCHET‑2 compare‑and‑append + thread‑mutex + `flock(2)`. AX‑15 is built.\n- `source/version-graph.lisp` — real bitemporal `[valid × recorded]`, `version-at`, live-at, `:suspensive`/`:resolutory` condition classes, replay chain (1000+ lines). AX‑08 is built.\n- `source/authority-evidence-replay.lisp` — real 626‑line producer-independent re-derivation (spans → normalization → merkle → RSA). AX‑05/AX‑13 built.\n- `systems/orchestrator-cli/capability-gate.lisp` + `deployment/verify/capability-baseline.sexp` (present) — real fail-closed read-only ratchet. AX‑20 gate built (kernel-admission wrapper is not).\n- `source/memory.lisp` — `verify-episode-chain` real; `deployment/self/episodes.sexp` is correctly gitignored/runtime-created, so its absence is **not** a defect. AX‑07 NOT flagged.\n- `authority-v2/proofs/*.py|*.sh` — real adversarial/negative-witness/topology scripts (AX‑09/AX‑10 gates), though several require privileged/Docker execution.\n\n## Bottom line\nThe infrastructure seats are real and strong. The **superset thesis (P1‑2)** and the **kernel `K` (P0‑1)** — the two things the word \"dominance\" actually rests on — are not established today: `K` is spec-only by explicit design and cannot be built in this SBCL repo without violating the creator's own one-seat law, and the generative capability needed for `⊋` over CoCounsel/Lexis/Harvey is unimplemented. Under the creator law (\"τίποτα μέτριο, κανένα μπάλωμα, τίμια άγνοια\"), the dossier should relabel every operational-`K` and \"executable defeater gate / signed-certificate\" claim as `DESIGN_ONLY / :blocked-toolchain`, and restate Section 4 containment as a target, not a fact."
]