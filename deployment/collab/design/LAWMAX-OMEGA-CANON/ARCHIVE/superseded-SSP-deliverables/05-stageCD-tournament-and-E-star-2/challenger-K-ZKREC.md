# K-ZKREC ASSESSMENT — Stage C Round 2

**Verdict: (b) MANDATORY ABSORPTION into E★★'s AX-13/AX-18 cost clauses. K-ZKREC is not a 10th challenger class.** The completeness critic's own caveat ("most natural disposition may be absorption") is correct, and the closure's v2 machinery proves it.

## 1. Why not a 10th class — the argument from the closure's own individuation rule

Challenger classes are individuated by family-signature differences that surface as **frozen guarantee-profile** deltas on the 22-axis instrument. K-ZKREC's entire delta against E★★ lives inside clauses the v2 reconciliation already adjudicated:

1. **AX-06 and AX-13 declare recompute-and-compare and transparent proof-of-recomputation EQUALLY MAXIMAL in assurance** under the same hash-assumption family (the MODE-EQUIV lemma in AX-13's lean obligation: the two verifiers accept the same set of (c,o) pairs). So on every assurance leg, K-ZKREC ties E★★ **by construction** — the tie is a theorem of the frozen instrument, not a tournament outcome to be discovered.
2. **AX-18's sublinear-verification clause is a RESOURCE_BOUNDED cost carve-out** ("its absence here is a declared-toolchain/arithmetization cost fact, which is exactly what the RESOURCE_BOUNDED kind records"). Convention (5) of the closure bars cost facts from ABSOLUTE labels; the same discipline bars a candidate whose only deltas are cost-clause deltas from constituting a distinct guarantee profile.
3. A 10th class that provably cannot generate a delta-satisfying gap on any frozen assurance axis against the join is not a challenger under the cap calculus — it is a **verification-transport configuration of E★★**. The tournament row would be a structural tie on all 22 frozen legs, differing only in appendices.

The "organizing principle" framing fails independently: a STARK receipt proves *the committed function was applied to committed inputs*. Every organizing decision — what gets committed (AX-04: the ABSOLUTE negative half stands, arithmetization creates zero origin authenticity after the fact), what the admission predicate is (H4), what UNKNOWN means (H2), which acceptance semantics (AX-01) — is **upstream** of the proof. K-ZKREC is a rim, not a spine; the closure already rejected the analogous "verified shield as organizing principle" collapse for K-NSV's verify-out shape. The seat for it already exists in the grammar: DIM-08 `proof-carrying-state-updates` with the CA P2-14 scope fence, plus AX-13/AX-18 mode-(b) clauses. What is missing is not a class but the mechanism specification — supplied below.

**Why the absorption is MANDATORY, not optional:** (i) the round-1 completeness defect is real — K-ZKREC appears in neither §3A nor §3B; (ii) AX-18's field-unreached cell ("no machine-checked scaling bound artifact exists," field max R3) is reachable **today** at the lower rungs, and CEILING-BAR/supreme law forbids leaving a reachable-superior conception unrecorded; (iii) it upgrades K-TLOG's countersigned receipt to self-verifying, materially widening the class of parties who can hold the institution to H8.

## 2. The mechanism: ZKREC-LADDER, three rungs bound into E★★'s AX-13/AX-18 cost clauses

**Z1 — Certified sublinear structural verification (no arithmetization; feasible now).** Machine-checked proof (Lean 4, the declared toolchain; the F*/HACL*-verified Merkle tree of the CCF lineage is the existing substrate-class precedent) that the RFC-9162-profile inclusion/consistency checker (`source/merkle-authority.lisp` seat) verifies membership and log extension in O(log n) hash invocations, with the verified checker differentially byte-tested against the Lisp seat. **Buys:** the exact missing AX-18 artifact type — a machine-checked scaling bound — for membership/consistency verification (field R3 → candidate R4 on the cost leg). **Trust added: none** (same SHA-256 family). Cost: weeks of proof engineering; consistent with the repo's Lean-obligation discipline (and honestly adjacent to the existing 0/17 proof debt). Caveat carried: Z1's grounding via an existing substrate-class mechanized proof is conditional on the round-2 **substrate-proof uniformity ruling** (the critic's re-steelman finding 2) — it must be ruled the same way as K-BFT's Velisarios-grounded AX-03 R5.

**Z2 — Succinct transition receipts for the admission spine (the DIM-08 family's own scope: update-validity predicates over bounded structures; feasible today).** A transparent STARK (hand-written AIR à la Winterfell/Plonky3-class, or a small Rust zkVM guest of a few thousand lines) proving per release: `head' = MerkleAppend(head, batch)` ∧ every batch leaf satisfies the decidable admission predicate (hash re-derivation, Ed25519 chain checks, `seq' = seq + 1` monotonicity) — the production zk-light-client shape. With per-release proofs chained (or recursively aggregated), any third party verifies the **entire accountable-transition history from genesis in polylog time** with no recompute capacity. **Buys:** AX-13 mode (b) becomes operative for the ledger layer (no assurance rung change — equally maximal by construction); AX-18's sublinear-history-verification clause moves from "not cryptographically excluded" to attained-for-the-spine; the DIM-09 K-TLOG portable receipt becomes **self-verifying**, widening the H8 verifier class from "parties holding an SBCL toolchain plus the full corpus" to "anyone with a ~100ms verifier binary." Prover cost: minutes-to-hours per release on one GPU machine — acceptable because releases are batch events, and AX-17's budget-gate machinery is exactly where the prover budget is committed.

**Z2 trust assumptions, stated honestly** (the ceilings' "same hash-assumption family" sentence is approximately true but incomplete):
1. Collision-resistant hashing — already assumed (genuinely same family; no trusted setup; plausibly post-quantum).
2. **Fiat-Shamir / random-oracle soundness of the non-interactive argument + conjectured (not proven) FRI soundness at production parameter choices** — an assumption *instance* the bare SHA-256 commitments never invoke. Plain commitments need only collision resistance; a STARK argument system needs ROM-heuristic soundness. Same *symmetric-crypto category*, new *instance* — must be a typed assumption in the certificate algebra, not folded silently into "same family."
3. **Implementation correctness of the circuit/prover** — no production zkVM or AIR toolchain is machine-checked end-to-end today; production zkVMs have had publicly disclosed soundness bugs. This is a large unverified Rust codebase entering the trust story. **Mitigation, mandatory:** dual-mode acceptance — the proof mode never *replaces* recompute-and-compare availability; every receipt remains recomputable, so the assurance floor is pinned at the recompute arm and the proof arm only lowers verifier cost. This dual-mode clause is what keeps the absorption consistent with AX-13's ABSOLUTE assurance label.

**Z3 — Whole-pipeline arithmetization: NOT feasible today for the actual B0; recorded as RESOURCE_BOUNDED frontier, never as mechanism.** The honest arithmetization census against `_stageB_master.md`: B0's trusted path is a **2,411-file SBCL Common Lisp system** — CLOS dispatch, macros, GC, cl-ppcre regex engines (including the `text-canonicalizer.lisp` canonical-text path), Unicode NFC, PDF parsing (`pdf-authority.lisp`), Greek NLP tokenization/lemmatization, RDF/Turtle generation across the omega modules, SHACL validation, lparallel, filesystem/OS interaction. There is **no SBCL→zkVM path**: zkVM guests are bare-metal rv32im with no OS, mmap, or signal machinery, which SBCL's runtime and GC require. The two real routes both fail today: (α) rewrite the trusted kernel in Rust — a full trusted-path rewrite colliding with the declared SBCL commitment (already recorded as a RESOURCE_BOUNDED scope choice at DIM-06's verified-kernel-chain note); (β) a small deterministic Lisp interpreter as the zkVM guest executing the kernel's committed source — semantically clean, but it stacks ~10–100× interpretation overhead on ~10⁴–10⁶× zkVM proving overhead: a native run of minutes (3000-event chain, corpus hashing, RDF generation) lands at 10¹⁰⁺ pre-interpretation guest cycles, i.e., GPU-days per release under today's provers. **Hence Z3's honest form: the provable perimeter is the admission/commit spine — which Z2 already covers — while parsing/extraction/NLP stay in recompute mode behind the AX-04 dual-digest byte-equality boundary. For B0 as it exists, "STARK receipts for every derivation" collapses to Z2-plus-recompute; claiming more would be the vendor-grade overclaim the freeze bans.**

## 3. Disposition instructions for the round-2 report

1. **§3B absorption entry (mandatory):** amend DIM-08 `proof-carrying-state-updates` with the member "self-verifying transition receipts (zk-light-client profile, dual-mode with recompute)"; amend DIM-09 `single-writer-plus-witness-quorum` so the portable countersigned receipt absorbs the succinct self-verifying strengthener.
2. **E★★ v2:** AX-13 and AX-18 cost clauses gain the Z1/Z2 attainment schedule as mandatory strengtheners; Z3 recorded as RESOURCE_BOUNDED frontier with the arithmetization census above; the Z2 assumption-instance and implementation-trust residues entered as typed assumptions in the certificate algebra.
3. **No 10th tournament row.** The AX-18 field maximum re-adjudicates only when Z1/Z2 replayable artifacts exist (evidence discipline: no rank without artifacts), and Z1's substrate-proof grounding waits on the uniformity ruling.
4. **Rung summary:** AX-18 cost leg R3 → R4 (Z1, now) → R5-candidate (Z2, months); AX-13 mode-(b) operative for the spine; AX-06 assurance unchanged (tie by theorem); K-TLOG receipt self-verifying (H8 verifier-class widening — a reach improvement, not a new rung); AX-04/AX-01/H4/H2 untouched — no organizing-principle claim survives.