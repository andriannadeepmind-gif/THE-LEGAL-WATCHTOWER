I have verified enough across the census and the real repo. All cited files exist and I have directly inspected the load-bearing and P0-adjacent ones. Here is the adjudication.

## Honesty-adversary verdict table

| # | capability | cited path | verdict | reason / real status |
|---|---|---|---|---|
| 1 | Defeasible reasoning w/ auto-retraction (JTMS) | orchestrator-cli/inference-gate.lisp | **CONFIRMED_EXECUTING** | Real JTMS: well-founded defeaters, repealed non-propagation, `:derived` proofs, cyclic `:out` termination. Census VERIFIED, no debt. Example-driven caveat is disclosed. |
| 2 | Cross-authority conflict resolution (lex rules) | legal-conflict-resolution.lisp + legal-counterfactual.lisp | **CONFIRMED_EXECUTING** | `defrule` lex-superior/specialis/posterior, symmetric, proof-carrying; counterfactual has critical-facts/minimal-blockers. Deterministic today. "Single relation A" is disclosed as future E★★. |
| 3 | Deterministic single-entry pipeline composition | orchestrator-spec/pipeline-dsl.lisp + core/dependency-graph.lisp | **CONFIRMED_EXECUTING** | `defpipeline` + `topological-sort` with cycle→`dependency-error` both real. Note: the *meta* pipeline-registry that reads these is P0-broken (registry.lisp "empty duplicate serves false answers"), but the DSL+sort seats themselves execute. |
| 4 | Honest "I don't know" (honest-ceiling judges) | orchestrator-cli/deontic-gate.lisp + inference-gate.lisp + meta-ontology.lisp | **CONFIRMED_EXECUTING** | Gate machinery VERIFIED; typed refusals present. 91.5% classifier heuristic + advisory-only boundary decls (BO-22) honestly disclosed. |
| 5 | Un-gameable positive proof census | authority-v2/proofs/verify-proof-manifest.py + run-proofs.sh | **CONFIRMED_EXECUTING** | Genuinely fail-closed: empty manifest→exit 1, load-bearing-not-proved w/ gate≠not-passed→false-green error, recursive scan rejects forgotten/baptised proofs. Sibling false-green emitters are elsewhere (P0-1), not this seat. |
| 6 | Mutation-kill adversarial self-testing | authority-v2/proofs/capture-mutation-witness.py | **CONFIRMED_EXECUTING** | Census DETERMINISTIC, no debt. Claim honestly scoped to capture/Merkle covered areas only; axis rank UNKNOWN disclosed. |
| 7 | Counterfactual ablation self-falsification | legal-counterfactual.lisp + introspection.lisp | **CONFIRMED_EXECUTING** | Ablation core (`what-if-report`, `minimal-blockers`) is real deterministic code. |
| 8 | Frozen adversarial regression suites | orchestrator-cli/deontic-gate.lisp + inference-gate.lisp | **CONFIRMED_EXECUTING** | Frozen suites behind VERIFIED gates. Classifier-retirement caveat disclosed; guarantee carried by gate/suite machinery. |
| 9 | Reproducible source-quote verification | source/authority-evidence-replay.lisp | **CONFIRMED_EXECUTING** | Census VERIFIED; recompute-source-bytes→extraction→byte-equality, trusts no declared root. |
| 10 | Corpus-size-independent membership proof | source/merkle-authority.lisp | **CONFIRMED_EXECUTING** | RFC-6962, domain-sep (0x00/0x01), unbalanced-split explicitly CVE-2012-2459-safe. VERIFIED. |
| 11 | Cryptographic authority-chain verification | source/authority-proof-bundle.lisp | **CONFIRMED_EXECUTING** | Ed25519 owner-pin, delegation-valid-at-genTime, revocation, RFC-7638 thumbprint. VERIFIED. |
| 12 | Time-anchored authority receipt (bitemporal) | source/legal-authority-receipt.lisp | **CONFIRMED_EXECUTING** | receipt-id = canonical hash of whole receipt; bitemporal; genTime anti-rollback. VERIFIED. |
| 13 | Strict DER/ASN.1 parsing | source/asn1-der.lisp | **CONFIRMED_EXECUTING** | Strict decoder rejects BER/indefinite-length/multi-byte tags/oversized length. Single seat. |
| 14 | No LLM in grounding/citation trust path | authority-proof-bundle.lisp + authority-evidence-replay.lisp | **CONFIRMED_EXECUTING** | Both VERIFIED deterministic; grounding verifiers are LLM-free (cross-cutting invariant). |
| 15 | Citation extraction from free text (heuristic) | source/citation-authority.lisp:224 | **CONFIRMED_EXECUTING (lossy)** | Runs, but line 224 `(<= num 120)` + integer-only node typing silently drops any article >120 and lettered articles (P0-10). Executes as disclosed defective/being-retired heuristic — materially wrong for any corpus beyond the 120-article constitution. |
| 16 | Tamper-evident inclusion & append-only proof | source/merkle-authority.lisp | **CONFIRMED_EXECUTING** | Same VERIFIED Merkle seat; inclusion/consistency recomputed from leaves. |
| 17 | Recompute-and-compare evidence lineage | source/authority-evidence-replay.lisp | **CONFIRMED_EXECUTING** | VERIFIED; dual digest + third-party byte-equality replay. |
| 18 | Hash-committed artifact inventory (census) | orchestrator-epistemic/artifact-census.lisp | **CONFIRMED_EXECUTING** | VERIFIED; `%prev-release-root` anti-equivocation chain + corpus-fingerprint real. |
| 19 | Human-readable source-provenance | source/provenance-link.lisp + core/source-detection.lisp | **CONFIRMED_EXECUTING** | provenance-link DETERMINISTIC/no-debt; source-detection exact-path w/ glob fallback disclosed as slated for deletion. |
| 20 | Triple independent offline re-verification | verify.py + verify.mjs + kernel-verify.lisp | **CONFIRMED_EXECUTING** | All three VERIFIED and substantial (225/141/218 lines); diverse re-implementations. |
| 21 | Fail-closed proof manifest | verify-proof-manifest.py + run-proofs.sh | **CONFIRMED_EXECUTING** | Same fail-closed machinery as #5; honestly reports 0/17 kernel proofs. |
| 22 | Release-spine re-verification in release identity | orchestrator-epistemic/release-spine.lisp | **CONFIRMED_EXECUTING** | Census VERIFIED, no debt. |
| 23 | Exact deterministic replay of a receipt | source/legal-authority-receipt.lisp | **CONFIRMED_EXECUTING** | VERIFIED; exact journal-prefix / exact-seq replay. |
| 24 | Canonical deterministic serialization | omega-modules/rdf-canonicalization.lisp + model/article.lisp | **CONFIRMED_EXECUTING** | Both DETERMINISTIC/VERIFIED; single canonical-ordering seat. |
| 25 | Cannot-think-invisibly (recorded trace) | source/deliberation.lisp + cli/provenance-gate.lisp | **CONFIRMED_EXECUTING** | MOP `thought-class` records at `initialize-instance :after` (real); provenance-gate registers traced ops. Caveat: MOP invariant structurally binds *thought objects*; universality over "every step" rests on provenance-gate registration, not the MOP alone. |
| 26 | Tamper-evident institutional memory log | source/memory.lisp | **CONFIRMED_EXECUTING** | Append-only SHA-256 chain, prev-hash, full-recompute verify. VERIFIED. |
| 27 | Downgrade-proof memory records | source/memory.lisp | **CONFIRMED_EXECUTING** | `:hv` inside sealed hash-committed body ([RATCHET-3]); lowering breaks leaf hash. VERIFIED. |
| 28 | Read-back-verified decision ledger | source/adoption-decision.lisp | **CONFIRMED_EXECUTING** | `require-durable!` + read-back SHA-256 present. P1 wall-clock disclosed (determinism debt, not a fail). |
| 29 | Matter-memory recall interface | orchestrator-cli/memory-commands.lisp | **CONFIRMED_EXECUTING** | Census VERIFIED CLI query seat. |
| 30 | Past-time replay of legal-authority state | source/legal-authority-receipt.lisp | **CONFIRMED_EXECUTING** | VERIFIED; partial (receipt path) disclosed. |
| 31 | Point-in-time version history | source/version-graph.lisp + tests/temporal-semantics-test.lisp | **CONFIRMED_EXECUTING** | Both VERIFIED. |
| 32 | Append-only knowledge-timeline proof | orchestrator-epistemic/transparency-log.lisp | **CONFIRMED_EXECUTING** | VERIFIED; RFC-6962 consistency proofs. P1 error-swallow in chain walker + "wholesale-replacement not caught" both disclosed. |
| 33 | Hash-chained knowledge-time spine | orchestrator-epistemic/artifact-census.lisp | **CONFIRMED_EXECUTING** | VERIFIED prev_release_root chain. |
| 34 | Anti-rollback floor on cited authorities | source/authority-proof-bundle.lisp | **CONFIRMED_EXECUTING** | VERIFIED genTime-floor; consumer-supplied floor disclosed as partial. |
| 35 | Auto-invalidation on law change | orchestrator-cli/inference-gate.lisp | **CONFIRMED_EXECUTING** | Repealed-node non-propagation in the VERIFIED JTMS gate. |
| 36 | Kernel-enforced sole writer | authority-v2/proofs/verify-capability-closure.sh + capability/identities.sh | **CONFIRMED_EXECUTING** | `setpriv` reuid + try_write probes yield real kernel EACCES for producer/reader on authority store. DETERMINISTIC. |
| 37 | One-and-only-one corpus write seat | source/write-authority.lisp + tests/architecture-multiplicity-test.lisp | **CONFIRMED_EXECUTING** | Single-seat structure + regression gate execute (GATED_PARTIAL). Non-atomic emit (P1) disclosed; the single-entry property holds. |
| 38 | Read-back-verified durable commit | source/journal.lisp + source/adoption-decision.lisp | **CONFIRMED_EXECUTING** | journal VERIFIED; require-durable! read-back real. P1 wall-clock disclosed. |
| 39 | Signed append-only decision ledger | source/adoption-decision.lisp | **CONFIRMED_EXECUTING** | Hash-chained append-only journal writes real. P1 wall-clock disclosed. |
| 40 | Unbypassable constitutional gate on every command | orchestrator-cli/constitutional-dispatch.lisp | **DEMOTE_TO_BUILD_OBLIGATION** | The `:around` dispatch barrier is genuinely unbypassable and executes — but the gate it enforces, `orchestrator.constitution:evaluate` (constitutional-gate.lisp:44-45), **fails open**: a rule that throws → `(values t nil)` = allowed (P0-2). So an unconstitutional action whose rule errors is silently permitted. Structural wrapper executes; fail-closed enforcement is a build obligation. |
| 41 | Capability closure (only trusted invocable) | source/capability-registry.lisp | **CONFIRMED_EXECUTING** | Census VERIFIED, no debt. |
| 42 | Fail-closed verification harness | authority-v2/run-proofs.sh + verify-proof-manifest.py | **CONFIRMED_EXECUTING** | Same genuine fail-closed machinery as #5/#21. |
| 43 | Bounded autonomous error handling + resume | source/autonomy.lisp | **CONFIRMED_EXECUTING** | `max-consecutive-errors` (default 3) ceiling + `run-mission :resume` agenda checkpoint present. |
| 44 | Circuit breaker on external deps | source/circuit-breaker.lisp | **CONFIRMED_EXECUTING** | States/registry/timeout real; `:system` time (determinism debt) disclosed. |
| 45 | Append-only release/transparency log | orchestrator-epistemic/transparency-log.lisp + artifact-census.lisp | **CONFIRMED_EXECUTING** | VERIFIED; P1 error-swallow disclosed. |
| 46 | Regression-proof upgrades (capability ratchet) | orchestrator-cli/capability-gate.lisp + artifact-census.lisp | **CONFIRMED_EXECUTING** | Ratchet ≥ baseline recompute from prev_release_root, fail-closed. Both VERIFIED. |
| 47 | Architecture-map equals running code | orchestrator-cli/architecture-gate.lisp + tests/dependency-contract-consistency-test.lisp | **CONFIRMED_EXECUTING** | Both VERIFIED (arch-gate P2 lexer-blindness only). |
| 48 | Retired features stay retired (tombstones) | orchestrator-spec/conditions.lisp + tests/architecture-multiplicity-test.lisp | **CONFIRMED_EXECUTING** | `retired-entrypoint` tombstone condition present; regression test VERIFIED. Caveat: test carries P1 SKIP-on-missing-seat silent-green — tombstone mechanism executes but the guarding test can go green on a skip. |
| 49 | Independent triple re-verification of evidence | verify.py + verify.mjs + kernel-verify.lisp | **CONFIRMED_EXECUTING** | Same VERIFIED triple as #20. |
| 50 | No LLM in the trusted path | source/cognition.lisp + orchestrator-cli/inference-gate.lisp | **CONFIRMED_EXECUTING** | Stage-1 LLM advisory only; default `execute-step`→`:unverified` (census P1) actually reinforces LLM-free trust; trusted gate VERIFIED. |
| 51 | Untrusted-generator (agentic) admission gate | authority-v2/capability/identities.sh + verify-capability-closure.sh | **CONFIRMED_EXECUTING (OS layer only)** | The physical OS single-writer / kernel-EACCES separation executes and is cited. But the "single admission predicate K" is spec-only (admission-model.sexp, no executable form, BO-02) and the referenced supreme constitutional gate fails open (P0-2) — both honestly disclosed in the claim. Executing part = OS separation. |
| 52 | Content-neutral crypto/receipt layer | transition-certificate.cddl + merkle-authority.lisp + authority-proof-bundle.lisp | **CONFIRMED_EXECUTING** | CDDL schema + two VERIFIED jurisdiction-agnostic crypto seats; no jurisdiction literals. |

## Summary

- **50 of 52 CONFIRMED_EXECUTING.** The census's VERIFIED/DETERMINISTIC labels held up on direct inspection; all cited files exist and the load-bearing crypto/proof/JTMS seats are substantial, not stubs.
- **1 DEMOTE_TO_BUILD_OBLIGATION — #40 "Unbypassable constitutional gate."** The `:around` barrier is genuinely unbypassable, but it rests on P0-2: `constitutional-gate.lisp:44-45` treats a rule-evaluation error as *allowed* (`fail-open`). The "must clear a constitutional check" safety guarantee is therefore not executing — an erroring rule silently permits the action.
- **2 material caveats flagged though not demoted:**
  - **#15 Citation extraction** executes but rests on P0-10 (line 224 `(<= num 120)` clamp + integer-only nodes) — silently correct *only* for the ≤120-article constitution, silently lossy for the Penal Code (art. 536, 100Α) and any larger corpus. Marked CONFIRMED-but-lossy because the claim self-discloses it; a strict reading of "pulls out the citations it mentions" is overstated for general corpora.
  - **#51 Untrusted-generator admission gate** executes only at the OS/kernel-EACCES layer; its "single admission predicate K" is spec-only (BO-02) and it references the same fail-open constitutional gate (P0-2). Both disclosed.
- **Honesty of the claim set:** disclosures were accurate wherever I checked — non-atomic emit (#37), wall-clock determinism debt (#28/#38/#39/#44), P1 error-swallow (#32/#45), 0/17 proofs (#21), and the classifier-retirement caveats were all real and correctly located. The one place the disclosure did not neutralize the claim is #40, where a VERIFIED dispatch wrapper is cited but the enforced gate carries a P0 fail-open the capability title ("unbypassable") directly contradicts.