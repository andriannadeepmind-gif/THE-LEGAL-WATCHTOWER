Adversarial review complete (lens: instrument-and-honesty, fresh context). I independently re-verified the corpus's in-repo citations before attacking: `source/constitutional-gate.lisp` fail-open `(error () (values t nil))`, `source/write-authority.lisp` `:if-exists :supersede`, `parallel-executor.lisp` error-swallow-then-`(setf (gethash stage-name executed) t)`, `citation-authority.lisp` `(<= num 120)` and `pagerank` at 408, `frbr-pipeline-stage.lisp` `(integer 1 120)` and the nil `report-stage-progress`, `witness-policy.sexp` disabled quorum, and the 9/17/13 theorem counts — all corroborated. The corpus does not lie about the repo. It lies (or overstates) about itself. Findings:

---

## P0

**P0-1 — The instrument's independence claim is false: K-SUP and the ceilings are tailored to B0 by construction, while the freeze and closure claim the opposite.**

Quoted claims: order_definition — "defined purely on the 22 frozen axis lattices and the hard invariants H1-H8, **with no reference to any existing design**"; closure K-SUP — "constructed as the dimension-wise join of the 22 ceilings' attainment_mechanisms, i.e., **a function of the FROZEN instrument alone, definable before any candidate (including B0) is examined**"; no_post_hoc — "generated by a mechanical rule … that a third party can recompute **from the frozen instruments alone**."

Contradicting text, from the same corpus: every ceiling's `attainment_mechanism` is a B0 file inventory — AX-01: "**Extend the existing JTMS engine behind systems/orchestrator-cli/inference-gate.lisp**…"; AX-02: "**B0 already holds every ingredient: the DEFPIPELINE DSL**…"; AX-04: "**The dual-digest seat already exists** … systems/orchestrator-epistemic/primary-anchor.lisp"; AX-13: "**Generalize the pattern B0 already runs**." The "mechanical rule" defining K-SUP is a join over B0's own completion roadmap. A join over B0-relative mechanisms is not definable "before B0 is examined" — B0 was examined to write every one of them, and 22/22 ceilings carry a `b0_gap` field, i.e., the ceilings were authored with B0 open on the desk.

Correction: split each ceiling into (a) a design-neutral guarantee statement + derivation (freezable), and (b) a non-frozen implementation appendix ("one attainment witness: B0 seats X, Y"). Redefine K-SUP as the join of the guarantee profiles (a) only. Delete the "definable before any candidate is examined" sentence, or replace with the honest form: "K-SUP is the ceiling-envelope; its attainment notes use B0 as one constructive witness, which biases mechanism choice but not the guarantee bound." The claim as written is exactly the post-hoc tailoring FOC-04 exists to exclude.

**P0-2 — The anti-pruning guarantee in `foc04_checklist` is false by string inspection of the corpus itself: most dominance-eliminations retain NO representative, so those families are eliminated by prose, never by verdict.**

Quoted claim (d_t_not_winner_only): "grammar-level PER_DIMENSION_DOMINANCE eliminations remove families from E**-candidate signatures but **every such elimination names its retained representative class in D_T** (K-COM for five eliminated families, K-RULES, K-CRDT, K-AGT), **so nothing eliminable vanishes before adjudication**."

String inspection: PER_DIMENSION_DOMINANCE eliminations with **no** retained representative named in their lemma: DIM-01 `recorded-unchecked-justification`, DIM-02 `declared-unenforced-policy`, DIM-03 `unvalidated-citation`, DIM-04 `atemporal-snapshot` and `timestamped-unused`, DIM-06 `prose-spec-only`, DIM-07 `stateless`, DIM-08 `no-provenance` and `post-hoc-model-influenceable-logs`, DIM-09 `emergent-gossip-detection`, DIM-11 `declared-plan-unchecked`, DIM-12 `heuristic-flagging`, DIM-13 `no-falsification` names K-COM but DIM-15's rep is K-B0 itself. That is ≥12 families removed from the candidate grammar on the strength of an unadjudicated prose lemma — directly contradicting the corpus's own rule that dominance is "adjudicated by full 22-axis verdicts, not presumed," and contradicting unknown_rule's spirit (elimination requires KNOWN ranks; a lemma is not a 22-record verdict set).

Correction: either (i) name a retained D_T representative for every dominance-eliminated family (many map naturally: K-HUM carries `stateless`/`no-provenance`/`heuristic-flagging`; K-COM carries `atemporal-snapshot`/`declared-plan-unchecked`; K-AGT carries `post-hoc-logs`) and amend the checklist to the true list, or (ii) reclassify representative-less eliminations as *conjectured dominance* — typed obligations that must be discharged by an actual verdict against a synthesized minimal representative before the family may be dropped from the completeness critic's class registry. Rewrite the quoted sentence to state what is actually true.

---

## P1

**P1-1 — UNKNOWN upgraded to verified: `VERIFIED_FROM_SOURCE` assigned to sources the sweep admits it never read, and those upgraded statuses then ground rank-like claims in the closure.**

Quoted self-admission (neurosymbolic area, itself tagged UNKNOWN_SOURCE_UNREACHABLE): "Direct fetches of arxiv.org, openreview.net, nature.com … were blocked … **all VERIFIED_FROM_SOURCE claims above were verified from source content surfaced through the WebSearch tool's rendered result excerpts** … not from full-text reads." Same pattern in commercial-baselines: "harvey.ai direct fetch proxy-blocked; content via WebSearch text" → status **VERIFIED_FROM_SOURCE**; AlphaProof: "Nature paper itself unreachable via proxy" → **VERIFIED_FROM_SOURCE**. Meanwhile structurally identical situations elsewhere honestly got UNKNOWN_SOURCE_UNREACHABLE (ICCMA 2025, MINGLE, Lean-to-C). The status lattice is applied inconsistently, always in the upgrading direction. The leak then propagates: closure K-COM — "**Note its genuine rungs: AX-05 ~R3** (assertion-level support checking), **AX-07 above R2**" — ranks asserted from vendor press-release text relayed by a search index, which the verdict_schema itself rules inadmissible: "**a reference to a producer's self-declaration is not admissible evidence**."

Correction: introduce and apply a third status, `VERIFIED_FROM_SECONDARY` (search-excerpt / vendor-declared), and demote every proxy-blocked "verified" claim to it; strike the "genuine rungs" sentences or retype them as "vendor-declared candidate rungs, UNKNOWN pending admissible evidence." Under the frozen unknown_rule, K-COM's AX-05/AX-07 are UNKNOWN today, not ~R3.

**P1-2 — Verdict schema contains a dead, self-contradictory clause and an unresolved redundancy that is a latent leak channel.**

Quoted: "**a KNOWN gap smaller than the delta_rule is TIE-with-noted-gap in justification, not a win**" versus "**TIE only when rank_a and rank_b are both KNOWN and equal**." Every axis's delta_rule fires at ≥1 full rank (AX-17/18 add a within-equal-rank class clause but also fire at 1 rank), so a KNOWN unequal-rank gap smaller than delta is impossible — the clause is unreachable; and if ever invoked it would force winner=TIE with unequal rank fields, violating the TIE definition in the same schema. Separately, "Global dominance … is recomputed by any reader as the pure conjunction over the 22 records" — but recomputed from *which* field? The `winner` field duplicates information derivable from `rank_a`/`rank_b`+delta_rule with no stated precedence on disagreement, and for AX-17/18 the decision-bearing complexity class exists **only as free prose in `justification`** — an untyped field deciding a win, in an instrument whose whole thesis is "prose is not machine-checked."

Correction (via the pre-registered S2 refinement mechanism, version-stamped, before first use): delete the TIE-with-noted-gap clause or restate it as applying only to within-rank class comparison on AX-17/18; declare that global dominance recomputes from rank fields plus the typed class fields, with `winner` a derived check bit whose mismatch invalidates the record; add a typed `complexity_class_a/b` field to AxisVerdict for AX-17/18.

**P1-3 — termination_rule T2 certifies an open-class negative the corpus's own ceiling proves uncertifiable.**

Quoted: T2's critic "certifies … that zero unexplored challenger CLASSES remain — i.e., every pre-registered generation class … has been instantiated and adjudicated, **and the critic could construct no new class**." The first conjunct is decidable (census over a committed finite registry — fine). The second is exactly what AX-10's ABSOLUTE ceiling forbids: "**No feasible system can certify refutation-completeness over the open class of all possible defects**," and the AX-09 derivation: "blind spots over inputs never received are undetectable in principle." A recorded, replayable search that found nothing establishes only that *that search* found nothing — failure-to-construct is UNKNOWN, and T2 as written treats that UNKNOWN as the pass that permits SUCCESS.

Correction: restate T2 as "completeness is certified **relative to the committed, hash-frozen generation-class registry**; the critic's failed construction attempt is recorded as corroboration, typed UNKNOWN beyond the registry, and the registry hash is part of the termination record" — mirroring exactly the committed-finite-class move AX-10's own ceiling makes for mutation classes.

**P1-4 — Ceilings assert ranks as established that are derived-but-unverified prose, violating the freeze's own rule.**

The freeze: "**an assessor's unverifiable claim of a rank is not a rank**." Yet: AX-07 b0_gap — "The chain, downgrade resistance, and recompute verification themselves are **already at ceiling grade** in source/memory.lisp"; AX-04 attainment — "The dual-digest seat already exists and is **supreme-grade**"; AX-14 b0_gap — "**B0 already attains the ceiling locally**"; AX-07 derivation — "so the ceiling is **attained, not merely approached**"; AX-04 b0_gap — witness-NIL "is **at-ceiling for declared resources**." All while AX-21 records 0/17 theorems proved and every lean_obligation undischarged; none of these components has recompute-and-compare third-party evidence of its rank. These are exactly the "derived-but-unverified prose stated as established" the task names.

Correction: rewrite each as a typed hypothesis — e.g., "memory.lisp's chain is a *candidate* ceiling-grade seat; rank UNKNOWN pending the T1–T4 obligations of AX-07's lean_obligation and an independent replay artifact." The "attained, not merely approached" sentence in the AX-07 derivation must move from the derivation (which should bound) to the attainment note (which may conjecture).

**P1-5 — Lattice tailoring, two concrete instances.**

(a) AX-10 R4 is a paraphrase of LAWMAX's own internal protocol, not a design-neutral rung: "independent adversarial agents with fresh context are an obligatory phase gate; every finding **closed at its seat**, refuted with proof, or **registered with a retirement phase**." "Closed at its seat" and "retirement phase" are CLAUDE.md/[0047] idioms («κλείνεται ΣΤΗΝ ΕΔΡΑ του … υπόλειμμα με φάση θανάτου»). The rung grades candidates by conformance to B0's process vocabulary. Correction: restate R4 in guarantee terms — "structurally independent adversarial review (no access to implementer rationale) is a blocking admission gate; every finding reaches a recorded terminal disposition (fixed, refuted-with-proof, or tracked-to-scheduled-removal)."

(b) AX-07 R3 / AX-12 R3 encode B0's *mechanism* rather than the guarantee: "**single-seat, single-writer** typed matter record with **capability closure (H4)**," and AX-07's delta: "R3+ requires evidence that the **single-writer property** is structurally enforced." The closure then admits K-BFT to U_T by glossing "H4 satisfied **at the logical-sequence level**" — a reading that appears nowhere in the frozen ladder text. Under the frozen text an assessor cannot assign K-BFT R3+ on AX-07/AX-12; under the closure it is an admissible E** candidate. That is a pre-loaded S2 within-axis incomparability (an instrument defect, by the corpus's own definition) that will structurally penalize exactly the non-B0-shaped admissible classes (K-BFT, and the retained K-CRDT comparison). Correction: before any verdict issues, run the incomparable_rule S2 lattice-refinement now — replace "single-writer" with "a single canonical serialization point (physical single writer, or consensus-established logical writer with machine-checked nonforking)," version-stamped.

---

## P2

**P2-1 — One missing-mechanism candidate silently dropped, falsifying "none was silently dropped."** no_post_hoc: "all ~60 missing_mechanism_candidates … are absorbed into named families with the absorption recorded inline (**verifiable by string inspection**) — **none was silently dropped**." String inspection: "**Transactional multi-agent argumentation**" (formal-legal-argumentation area) is absorbed by name in no family. Correction: record its absorption (DIM-10 durable-execution ∘ DIM-05 arbitration composite) inline, and weaken the checklist sentence to match reality after audit.

**P2-2 — Overstated cryptographic lower bounds stated as established.** AX-04: "commitment-based membership **needs Θ(log n)** with Merkle trees … **nothing structurally better exists without trusted state**" — constant-size membership proofs exist (RSA accumulators, vector commitments); transparent variants need no per-item trusted state. AX-18: "sublinear verification requires succinct proof systems (SNARKs) **whose trusted setup** … fall outside feasible" — STARK-class systems are transparent (no trusted setup); the honest exclusion ground is toolchain/arithmetization cost only. Corrections: "O(log n) via Merkle suffices and is optimal *among hash-based schemes with no setup assumptions*"; "excluded on toolchain-feasibility grounds, not on trusted-setup grounds."

**P2-3 — AX-01 uniqueness claim underivable as stated.** "the grounded/well-founded semantics is **the unique maximal semantics** that is simultaneously deterministic, polynomial-time computable, and certificate-carrying." Uniqueness over all semantics is asserted, not derived — ideal and eager semantics are also unique/deterministic; they fail only the polynomial leg (co-NP-hard verification), which the derivation never argues. Correction: "grounded is *a* semantics with all three properties; ideal/eager retain uniqueness but lose polynomial checkability (co-NP-hardness), so grounded is maximal among the tractable unique semantics" — same conclusion, honest derivation.

**P2-4 — Theorem-count conflation.** AX-01 b0_gap: "admission theorems **T1-T9** in authority-v2/kernel/admission-model.sexp are **0/17 proved**." Verified in-repo: admission-model.sexp holds exactly 9 theorems (0/9); 17 is the authority-v2-wide count (LEVEL7 matrix: "17 θεωρήματα, 0 proved") and 0/13 is Level-7. Correction: "T1–T9: 0/9 proved (part of the repo-wide 0/17 proof debt)."

**P2-5 — Unverified empirical premise in AX-04's attainment stated as fact.** "verify the National Printing House **PAdES/CMS signature embedded in ΦΕΚ PDFs**" — whether current ΦΕΚ PDFs carry a verifiable PAdES/CMS chain is an empirical claim about the publisher, carried with no evidence status; if false, the transport arm of the ceiling triple collapses to quorum-only for Greece. Correction: tag it as an unverified capture-time hypothesis with a typed fallback ("if unsigned → clause (i) corroboration path only").

**P2-6 — AX-11 taxonomy universal.** "write-ahead logging and shadow-paging are **the only two families**, and both bottom out in a single atomic pointer/record switch" — a folk-theorem stated as established (soft-updates/copy-on-write log-structured designs are arguable third points, even if reducible). Correction: "every known correct protocol family reduces to a single atomic switch (WAL, shadow-paging, and their CoW/log-structured hybrids); the reduction is the load-bearing claim and is what the lean_obligation must prove."

---

**Summary verdict:** the frozen *ladders* are largely design-neutral (two tailoring exceptions, P1-5); the *verdict schema* has no aggregate field but one dead-contradictory clause and one untyped decision-bearing datum (P1-2); UNKNOWN-as-pass occurs at three sites — frontier status upgrades (P1-1), termination T2's open-class certification (P1-3), and rank-prose in ceilings (P1-4); and the honesty core has two checkable-and-false self-certifications (P0-1, P0-2), which are the highest-severity items precisely because they live in the sections whose job is to prove the rest isn't rigged. All in-repo factual citations I sampled were accurate.