# =============================================================================
# Η ΜΙΑ ΔΗΛΩΜΕΝΗ ΠΗΓΗ ΑΛΗΘΕΙΑΣ: σουίτες tests/*-test.lisp που ΔΕΝ τρέχουν ως
# gate στο standalone-test stage ([audit#2]).
# =============================================================================
# Διαβάζεται ΚΑΙ από docker/run-standalone-suites.sh (τι τρέχει) ΚΑΙ από
# docker/verify-proof-manifest.py (τι αναμένεται στο manifest) — ΜΙΑ έδρα, καμία
# δεύτερη χειρόγραφη λίστα που ξεσυγχρονίζεται. Το suite inventory ΠΑΡΑΓΕΤΑΙ από
# το filesystem (glob tests/*-test.lisp) μείον αυτές τις ρητές εξαιρέσεις — άρα
# ΚΑΜΙΑ νέα σουίτα δεν μπορεί να «ξεχαστεί» (ο κριτής βρήκε 7 ξεχασμένες).
#
# Μορφή: ένα suite-basename ανά γραμμή (χωρίς το «-test» suffix), προαιρετικό
# σχόλιο με «#». Κάθε εξαίρεση ΠΡΕΠΕΙ να έχει λόγο.
#
# comparison: διαγνωστικό που απαιτεί Python reference fixture ΑΠΟΝ από το
#             SBCL-only standalone stage (τρέχει αλλού με python παρόν).
comparison
